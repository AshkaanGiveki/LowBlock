"use client";
import Link from "next/link";
import { useEffect, useMemo, useRef, useState } from "react";
import { ArrowUpRight, Crown, LoaderCircle, Medal, Sparkles } from "lucide-react";
import { useLanguage } from "@/components/LanguageProvider";
import { formatNumber } from "@/lib/text";
type Row = { userId: string; username: string; avatarUrl: string | null; points: number; exact: number; predictions: number; cursor?: { points: number; exact: number; predictions: number; userId: string } };
export function InfiniteLeaderboard({ initialRows, seasonStartYear, leagueCode, lifetime, me }: { initialRows: Row[]; seasonStartYear: number; leagueCode?: string; lifetime?: boolean; me: string | null }) { const { language, t } = useLanguage(); const [rows, setRows] = useState(initialRows); const [cursor, setCursor] = useState(initialRows.at(-1)?.cursor ?? null); const [loading, setLoading] = useState(false); const [done, setDone] = useState(initialRows.length < 100); const sentinel = useRef<HTMLDivElement>(null); const params = useMemo(() => { const query = new URLSearchParams({ limit: "40" }); if (!lifetime) query.set("seasonStartYear", String(seasonStartYear)); if (leagueCode) query.set("leagueCode", leagueCode); return query; }, [seasonStartYear, leagueCode, lifetime]); useEffect(() => { const node = sentinel.current; if (!node) return; const observer = new IntersectionObserver(entries => { if (entries[0]?.isIntersecting && cursor && !loading && !done) loadMore(); }, { rootMargin: "500px" }); observer.observe(node); return () => observer.disconnect(); }, [cursor, loading, done]); async function loadMore() { setLoading(true); const query = new URLSearchParams(params); query.set("cursor", btoa(JSON.stringify(cursor))); const response = await fetch(`/api/leaderboards?${query}`); const data = await response.json(); const incoming: Row[] = data.rows ?? []; setRows(current => { const ids = new Set(current.map(row => row.userId)); return [...current, ...incoming.filter(row => !ids.has(row.userId))]; }); setCursor(data.nextCursor ? JSON.parse(atob(data.nextCursor)) : null); setDone(!data.nextCursor || incoming.length === 0); setLoading(false); } const n = (value: number) => formatNumber(value, language); const podium = rows.slice(0, 3); return <section className="mt-8"><div className="grid gap-3 md:grid-cols-3">{podium.map((row, index) => <Link key={row.userId} href={`/u/${encodeURIComponent(row.username)}`} className={`rounded-2xl border p-5 text-center transition hover:-translate-y-1 ${index === 0 ? "border-[#e8c66a]/40 bg-[#e8c66a]/10" : "border-white/10 bg-[#101512]"}`}><span className="mx-auto grid h-12 w-12 place-items-center rounded-full bg-black/20 text-[#e8c66a]"><Crown size={index === 0 ? 21 : 17}/></span><b className="mt-3 block truncate">{row.username}</b><span className="mt-1 block text-xs text-[var(--muted)]">#{index + 1} · {n(row.predictions)} {t("پیش‌بینی", "picks")}</span><strong className="mt-3 inline-flex items-center gap-1 text-xl text-brand"><Sparkles size={15}/>{n(row.points)}</strong></Link>)}</div><div className="mt-5 overflow-hidden rounded-3xl border border-white/10 bg-[#101512]">{rows.map((row, index) => <Link key={row.userId} href={`/u/${encodeURIComponent(row.username)}`} className={`flex items-center gap-3 border-b border-white/[.06] px-4 py-4 transition hover:bg-white/[.03] ${row.userId === me ? "bg-brand/10" : ""}`}><span className="w-7 text-center text-sm font-black text-[var(--muted)]">{index + 1}</span><span className="grid h-10 w-10 shrink-0 place-items-center overflow-hidden rounded-full bg-brand/15 text-xs font-black">{row.avatarUrl ? <img src={row.avatarUrl} alt="" className="h-full w-full object-cover"/> : row.username.slice(0, 2).toUpperCase()}</span><span className="min-w-0 flex-1"><b className="block truncate">{row.username}</b><small className="text-[10px] text-[var(--muted)]">{n(row.exact)} {t("دقیق", "exact")} · {n(row.predictions)} {t("پیش‌بینی", "picks")}</small></span><span className="flex items-center gap-2 text-sm font-black text-brand">{n(row.points)}<ArrowUpRight size={15}/></span></Link>)}<div ref={sentinel} className="flex min-h-16 items-center justify-center p-4 text-xs text-[var(--muted)]">{loading ? <LoaderCircle size={18} className="animate-spin text-brand"/> : done ? t("پایان جدول", "End of leaderboard") : <Medal size={18} className="text-brand"/>}</div></div></section>; }
