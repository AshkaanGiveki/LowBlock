"use client";

import { useEffect, useState } from "react";
import Highcharts from "highcharts";
import HighchartsReact from "highcharts-react-official";
import { AnimatePresence, motion } from "motion/react";
import { BarChart3, GripHorizontal, Trophy, Users, X } from "lucide-react";
import { useLanguage } from "@/components/LanguageProvider";
import { formatNumber } from "@/lib/text";
import { teamName } from "@/lib/football/team-names";

export function MatchAnalytics({ matchId, onClose }: { matchId: string; onClose: () => void }) {
  const { language, t } = useLanguage();
  const [data, setData] = useState<any>(null);
  const [visible, setVisible] = useState(true);
  const number = (value: number) => formatNumber(value, language, { maximumFractionDigits: 1 });
  const score = (home: number | null, away: number | null) => home === null || away === null ? "—" : `${number(home)} - ${number(away)}`;
  useEffect(() => { fetch(`/api/matches/${matchId}/analytics`).then((response) => response.ok ? response.json() : null).then(setData).catch(() => setData(null)); }, [matchId]);
  const close = () => { setVisible(false); window.setTimeout(onClose, 280); };
  const options = data ? { chart: { type: "bar", backgroundColor: "transparent", height: 250, animation: { duration: 700 } }, title: { text: undefined }, xAxis: { categories: data.distribution.map((item: any) => String(item.score).split("-").map((part: string) => number(Number(part))).join(" - ")), lineColor: "transparent", tickColor: "transparent", labels: { style: { color: "#c5d0ca", fontFamily: "IranSans", fontSize: "11px" } } }, yAxis: { title: { text: undefined }, allowDecimals: false, gridLineColor: "rgba(255,255,255,.06)", labels: { formatter(this: Highcharts.AxisLabelsFormatterContextObject) { return formatNumber(Number(this.value), language); }, style: { color: "#98a59e", fontFamily: "IranSans" } } }, plotOptions: { series: { animation: { duration: 850 }, borderWidth: 0, borderRadius: 6, pointPadding: .16, groupPadding: .08 } }, legend: { enabled: false }, credits: { enabled: false }, series: [{ name: t("تعداد", "Count"), data: data.distribution.map((item: any) => item.count), color: "#20b879" }], tooltip: { backgroundColor: "#111713", borderWidth: 0, borderRadius: 12, style: { color: "#fff", fontFamily: "IranSans" }, formatter(this: any) { return `<b>${this.x}</b><br/>${formatNumber(Number(this.y), language)} ${t("پیش‌بینی", "predictions")}`; } } } : null;
  return <AnimatePresence>{visible && <div className="fixed inset-0 z-[90] flex items-end justify-center p-3 md:items-center md:p-6"><motion.button initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} onClick={close} className="absolute inset-0 bg-black/75 backdrop-blur-sm" aria-label={t("بستن", "Close")}/><motion.section drag="y" dragConstraints={{ top: 0, bottom: 0 }} dragElastic={{ top: .04, bottom: .28 }} onDragEnd={(_, info) => { if (info.offset.y > 105 || info.velocity.y > 650) close(); }} initial={{ y: "100%", opacity: 0, scale: .97 }} animate={{ y: 0, opacity: 1, scale: 1 }} exit={{ y: "100%", opacity: 0 }} transition={{ type: "spring", stiffness: 280, damping: 28, mass: .8 }} className="scrollbar-thin relative max-h-[90vh] w-full max-w-2xl overflow-y-auto rounded-[2rem] border border-white/10 bg-[linear-gradient(145deg,#14231b,#0b100d)] px-5 pb-8 pt-16 shadow-2xl md:px-8 md:pt-[4.5rem]"><div className="pointer-events-none absolute left-1/2 top-3 -translate-x-1/2 text-white/25"><GripHorizontal size={28}/></div><button onClick={close} className="absolute left-5 top-5 z-10 grid h-10 w-10 place-items-center rounded-2xl border border-white/10 bg-white/[.07] text-white/80 shadow-lg transition hover:border-brand/50 hover:bg-brand/15 hover:text-brand" aria-label={t("بستن", "Close")}><X size={19}/></button>{data ? <><div className="flex items-center gap-3"><Crest team={data.match.homeTeam}/><div className="min-w-0 flex-1 text-center"><p className="inline-flex items-center gap-1 text-[10px] font-black tracking-[.2em] text-brand"><BarChart3 size={14}/>{t("تحلیل مسابقه", "MATCH ANALYTICS")}</p><h2 className="mt-2 truncate text-lg font-black md:text-xl">{teamName(language, data.match.homeTeam.id, data.match.homeTeam.name)} <span className="mx-1 text-brand">·</span> {teamName(language, data.match.awayTeam.id, data.match.awayTeam.name)}</h2></div><Crest team={data.match.awayTeam}/></div><div className="mt-6 grid grid-cols-3 gap-2"><Stat icon={<Users size={15}/>} value={number(data.total)} label={t("پیش‌بینی", "predictions")}/><Stat icon={<Trophy size={15}/>} value={number(data.averagePoints)} label={t("میانگین امتیاز", "average points")}/><Stat icon={<Trophy size={15}/>} value={score(data.match.homeGoals, data.match.awayGoals)} label={t("نتیجه واقعی", "final result")}/></div><div className="mt-5 rounded-2xl border border-white/[.06] bg-black/20 p-4"><h3 className="mb-2 text-sm font-black">{t("توزیع نتایج پیش‌بینی‌شده", "Predicted score distribution")}</h3>{options && <HighchartsReact highcharts={Highcharts} options={options}/>}</div><div className="mt-6 space-y-2"><h3 className="text-sm font-black">{t("پیش‌بینی کاربران", "User predictions")}</h3>{data.users.map((user: any, index: number) => <motion.div key={`${user.username}-${index}`} initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: index * .045 }} className="flex items-center gap-3 rounded-2xl border border-white/[.05] bg-black/20 px-3 py-3"><span className="grid h-9 w-9 shrink-0 place-items-center overflow-hidden rounded-full bg-brand/15 text-[10px] font-black">{user.avatarUrl ? <img src={user.avatarUrl} alt="" className="h-full w-full object-cover"/> : user.username.slice(0, 2).toUpperCase()}</span><span className="min-w-0 flex-1 truncate text-sm font-bold">{user.username}</span><span className="text-xs text-[var(--muted)]">{number(user.points)} {t("امتیاز", "pts")}</span><b className="text-brand">{score(user.homeGoals, user.awayGoals)}</b></motion.div>)}</div></> : <AnalyticsSkeleton/>}</motion.section></div>}</AnimatePresence>;
}

function Crest({ team }: { team: any }) { return <img src={team.logoUrl ?? "/icon.png"} alt="" className="h-12 w-12 object-contain"/>; }
function Stat({ icon, value, label }: { icon: React.ReactNode; value: string | number; label: string }) { return <div className="rounded-2xl border border-white/[.05] bg-black/20 p-3"><span className="text-brand">{icon}</span><b className="mt-1 block text-lg">{value}</b><small className="text-[10px] text-[var(--muted)]">{label}</small></div>; }
function AnalyticsSkeleton() { return <div className="animate-pulse space-y-5"><div className="mx-auto h-7 w-2/3 rounded bg-white/10"/><div className="grid grid-cols-3 gap-2"><div className="h-20 rounded-2xl bg-white/10"/><div className="h-20 rounded-2xl bg-white/10"/><div className="h-20 rounded-2xl bg-white/10"/></div><div className="h-64 rounded-2xl bg-white/10"/><div className="h-14 rounded-2xl bg-white/10"/><div className="h-14 rounded-2xl bg-white/10"/></div>; }
