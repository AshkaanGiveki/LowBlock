"use client";

import { useCallback, useEffect, useMemo, useRef, useState } from "react";
import { motion } from "motion/react";
import { Check, Globe2 } from "lucide-react";
import { usePathname, useRouter } from "next/navigation";
import { LEAGUES } from "@/lib/football/leagues";
import { MatchAnalyticsCard } from "@/components/MatchAnalyticsCard";
import { useLanguage, T } from "@/components/LanguageProvider";

type Match = React.ComponentProps<typeof MatchAnalyticsCard>["match"] & { leagueCode: string };
type Tab = "open" | "started";
type Cursor = { globalPriority: 0 | 1; kickoffAt: string; providerMatchId: string } | null;
type MatchPage = { matches: Match[]; predictions: Record<string, { homeGoals: number; awayGoals: number }>; nextCursor: Cursor; hasMore: boolean };

function stateOf(match: Match, now: number) { const kickoff = new Date(match.kickoffAt).getTime(); const finished = match.status === "FINISHED" || (now >= kickoff + 120 * 60_000 && match.status !== "POSTPONED"); const live = !finished && (match.status === "LIVE" || (match.status === "SCHEDULED" && now >= kickoff)); return { finished, live, started: live || finished || match.status === "SUSPENDED" }; }
function encodeCursor(cursor: Exclude<Cursor, null>) { return btoa(JSON.stringify(cursor)); }
function isGlobal(code: string) { return Boolean(LEAGUES.find((league) => league.code === code)?.globalLeaderboard); }

export function MatchesView({ matches: initialMatches, predictions: initialPredictions, nextCursor: initialCursor, hasMore: initialHasMore, initialSelectedLeagues = [], initialShowAllMatches = false, focusMatchId, initialTab = "open" }: { matches: Match[]; predictions: Record<string, { homeGoals: number; awayGoals: number }>; nextCursor?: Cursor; hasMore?: boolean; initialSelectedLeagues?: string[]; initialShowAllMatches?: boolean; focusMatchId?: string; initialTab?: Tab }) {
  const { t } = useLanguage(); const pathname = usePathname(); const router = useRouter();
  const [matches, setMatches] = useState<Match[]>(initialMatches); const [predictions, setPredictions] = useState(initialPredictions); const [nextCursor, setNextCursor] = useState<Cursor>(initialCursor ?? null); const [hasMore, setHasMore] = useState(initialHasMore ?? false); const [loadingMore, setLoadingMore] = useState(false); const [selected, setSelected] = useState<string[]>(initialSelectedLeagues); const [showAllMatches, setShowAllMatches] = useState(initialShowAllMatches); const [tab, setTab] = useState<Tab>(initialTab);
  const loadLock = useRef(false); const loadSentinel = useRef<HTMLDivElement>(null);
  const availableLeagues = useMemo(() => LEAGUES.filter((league) => matches.some((match) => match.leagueCode === league.code)), [matches]);
  const filtered = useMemo(() => { const now = Date.now(); return matches.filter((match) => (showAllMatches || selected.includes(match.leagueCode)) && (tab === "open" ? !stateOf(match, now).started : stateOf(match, now).started)).sort((a, b) => Number(!isGlobal(a.leagueCode)) - Number(!isGlobal(b.leagueCode)) || new Date(a.kickoffAt).getTime() - new Date(b.kickoffAt).getTime() || a.providerMatchId.localeCompare(b.providerMatchId)); }, [matches, selected, showAllMatches, tab]);
  const savePreference = useCallback(async (payload: object) => { await fetch("/api/tournament-preferences", { method: "PUT", headers: { "content-type": "application/json" }, body: JSON.stringify(payload) }); }, []);
  const toggleLeague = (code: string) => { setShowAllMatches(false); setSelected((current) => current.includes(code) ? current.filter((value) => value !== code) : [...current, code]); void savePreference({ showAllMatches: false }); };
  const showAll = () => { setShowAllMatches(true); void savePreference({ showAllMatches: true }); };
  const loadMore = useCallback(async () => { if (!hasMore || !nextCursor || loadLock.current) return; loadLock.current = true; setLoadingMore(true); try { const params = new URLSearchParams({ cursor: encodeCursor(nextCursor), limit: "30" }); const response = await fetch(`/api/matches?${params.toString()}`, { cache: "no-store" }); if (!response.ok) throw new Error("matches unavailable"); const page = await response.json() as MatchPage; setMatches((current) => { const byId = new Map(current.map((match) => [match.providerMatchId, match])); for (const match of page.matches) byId.set(match.providerMatchId, match); return [...byId.values()]; }); setPredictions((current) => ({ ...current, ...page.predictions })); setNextCursor(page.nextCursor); setHasMore(page.hasMore); } catch { setHasMore(false); } finally { loadLock.current = false; setLoadingMore(false); } }, [hasMore, nextCursor]);
  useEffect(() => setTab(initialTab), [initialTab]);
  useEffect(() => { const node = loadSentinel.current; if (!node) return; const observer = new IntersectionObserver((entries) => { if (entries.some((entry) => entry.isIntersecting)) void loadMore(); }, { rootMargin: "800px 0px" }); observer.observe(node); return () => observer.disconnect(); }, [loadMore]);
  useEffect(() => { if (!focusMatchId) return; const timer = window.setTimeout(() => document.querySelector(`[data-match-id="${CSS.escape(focusMatchId)}"]`)?.scrollIntoView({ behavior: "smooth", block: "center" }), 80); return () => window.clearTimeout(timer); }, [focusMatchId, tab, filtered.length]);
  const changeTab = (next: Tab) => { setTab(next); const query = new URLSearchParams(window.location.search); query.set("tab", next); router.replace(`${pathname}?${query.toString()}`, { scroll: false }); };
  return <main className="desktop-predict-page min-h-screen px-4 pb-28 pt-24 md:px-8 md:pt-32"><div className="mx-auto max-w-7xl"><div className="desktop-page-heading mb-7"><p className="text-xs font-black tracking-[.18em] text-brand">{t("مسابقه‌ها", "MATCHES")}</p><h1 className="mt-2 text-3xl font-black md:text-5xl">{t("پیش‌بینی خود را ثبت کنید", "Make your prediction")}</h1><p className="mt-3 max-w-2xl text-sm leading-8 text-[var(--muted)]">{t("یک نتیجه برای هر مسابقه ثبت کنید.", "Make one prediction for each fixture and compete across every LowBlock leaderboard.")}</p></div><section className="mb-6 rounded-[1.75rem] border border-white/[.08] bg-[#101512] p-3 md:p-4"><div className="mb-3 flex items-center justify-between px-1"><p className="text-[10px] font-black tracking-[.18em] text-[var(--muted)]">{t("وضعیت", "MATCH STATUS")}</p><span className="text-xs text-brand">{tab === "open" ? t("آماده پیش‌بینی", "Prediction open") : t("زنده و نتایج", "Live & results")}</span></div><div className="grid grid-cols-2 gap-1 rounded-2xl border border-white/[.07] bg-black/20 p-1">{(["open", "started"] as const).map((value) => <button key={value} type="button" onClick={() => changeTab(value)} className={`relative rounded-xl px-2 py-3 text-center text-sm font-black ${tab === value ? "text-white" : "text-[var(--muted)]"}`} aria-pressed={tab === value}>{tab === value && <motion.span layoutId="matches-tab" className="absolute inset-0 rounded-xl bg-brand" />}<span className="relative z-10"><T fa={value === "open" ? "مسابقه‌های پیش‌رو" : "زنده و نتایج"} en={value === "open" ? "Upcoming matches" : "Live & results"} /></span></button>)}</div></section><div className="desktop-league-filter mb-8 flex flex-wrap gap-2"><motion.button type="button" aria-label={t("همه مسابقه‌ها", "All matches")} aria-pressed={showAllMatches} onClick={showAll} className={`grid h-12 w-12 place-items-center rounded-lg border ${showAllMatches ? "border-brand bg-brand/15 text-brand" : "border-white/10 bg-white/[.03] text-white/60"}`}><Globe2 size={19} /></motion.button>{availableLeagues.map((league, index) => { const on = showAllMatches || selected.includes(league.code); return <motion.button key={league.code} type="button" initial={{ opacity: 0, scale: .8 }} animate={{ opacity: 1, scale: 1 }} transition={{ delay: index * .055 }} aria-pressed={on} aria-label={league.enName} onClick={() => toggleLeague(league.code)} className={`relative grid h-12 w-12 place-items-center rounded-lg border ${on ? "border-brand bg-brand/15" : "border-white/10 bg-white/[.03]"}`}><img src={league.logo} alt="" className="h-7 w-7 object-contain" />{on && <span className="absolute -right-1 -top-1 grid h-4 w-4 place-items-center rounded-full bg-brand"><Check size={11} /></span>}</motion.button>; })}</div>{filtered.length ? <div className="desktop-match-grid grid gap-5 xl:grid-cols-2">{filtered.map((match, index) => <motion.div key={match.providerMatchId} data-match-id={match.providerMatchId} initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: index * .045 }}><MatchAnalyticsCard match={match} initial={predictions[match.providerMatchId]} index={index} openOnMount={focusMatchId === match.providerMatchId} /></motion.div>)}</div> : <div className="rounded-xl border border-dashed border-white/15 p-14 text-center text-sm text-[var(--muted)]">{t("مسابقه‌ای برای این فیلتر نیست.", "No matches match this filter.")}</div>}<div ref={loadSentinel} className="flex min-h-16 items-center justify-center py-5 text-xs text-[var(--muted)]" aria-live="polite">{loadingMore ? t("در حال بارگیری…", "Loading more matches…") : hasMore ? t("برای مشاهده بیشتر اسکرول کنید", "Scroll for more matches") : ""}</div></div></main>;
}
