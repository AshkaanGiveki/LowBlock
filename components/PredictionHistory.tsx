"use client";
import{useState}from"react";import{Clock3,BarChart3,Trophy}from"lucide-react";import{useLanguage}from"@/components/LanguageProvider";import{teamName}from"@/lib/football/team-names";import{TeamCrest}from"@/components/TeamCrest";import{MatchAnalytics}from"@/components/MatchAnalytics";
type Row={matchId:string;kickoffAt:string;homeTeam:{id:number;name:string;logoUrl:string|null};awayTeam:{id:number;name:string;logoUrl:string|null};predictedHome:number;predictedAway:number;actualHome:number|null;actualAway:number|null;points:number;category:string};
export function PredictionHistory({rows}:{rows:Row[]}){const{language,t}=useLanguage();const[selected,setSelected]=useState<string|null>(null);return <main className="min-h-screen px-4 pb-28 pt-24 md:px-8 md:pt-32"><div className="mx-auto max-w-5xl"><section className="rounded-3xl border border-white/10 bg-[linear-gradient(135deg,#17261f,#0c100e)] p-6"><p className="text-xs font-black tracking-[.18em] text-brand">{t("پیش‌بینی‌های من","MY PREDICTIONS")}</p><h1 className="mt-2 text-3xl font-black">{t("سابقه شما","Your history")}</h1><div className="mt-4 flex items-center gap-2 text-brand"><Trophy size={17}/><b>{rows.reduce((s,r)=>s+r.points,0)}</b><span className="text-xs text-[var(--muted)]">{t("امتیاز کل","total points")}</span></div></section>{rows.length?<div className="mt-5 space-y-3">{rows.map(row=><button type="button" key={row.matchId} onClick={()=>row.actualHome!==null&&setSelected(row.matchId)} className="block w-full rounded-2xl border border-white/10 bg-[#101512] p-4 text-start transition hover:border-brand/50"><div className="flex items-center justify-between text-[10px] text-[var(--muted)]"><span className="flex items-center gap-1"><Clock3 size={12}/>{new Date(row.kickoffAt).toLocaleString(language==="fa"?"fa-IR":"en-GB")}</span>{row.actualHome!==null&&<span className="flex items-center gap-1 text-brand"><BarChart3 size={13}/>{t("مشاهده تحلیل","View analytics")}</span>}</div><div className="mt-4 grid grid-cols-[1fr_auto_1fr] items-center gap-3 text-center"><Team id={row.homeTeam.id} name={row.homeTeam.name} logo={row.homeTeam.logoUrl} language={language}/><span className="rounded-full border border-brand/30 px-2 py-1 text-[10px] font-black text-brand">VS</span><Team id={row.awayTeam.id} name={row.awayTeam.name} logo={row.awayTeam.logoUrl} language={language}/></div><div className="mt-4 grid grid-cols-3 gap-2 text-center text-xs"><span><small className="block text-[var(--muted)]">{t("پیش‌بینی","Prediction")}</small><b>{row.predictedHome} - {row.predictedAway}</b></span><span><small className="block text-[var(--muted)]">{t("نتیجه","Result")}</small><b>{row.actualHome===null?"—":`${row.actualHome} - ${row.actualAway}`}</b></span><span><small className="block text-[var(--muted)]">{t("امتیاز","Points")}</small><b className="text-brand">+{row.points}</b></span></div></button>)}</div>:<div className="mt-6 rounded-xl border border-dashed border-white/15 p-14 text-center">{t("هنوز پیش‌بینی ندارید","No predictions yet")}</div>}{selected&&<MatchAnalytics matchId={selected} onClose={()=>setSelected(null)}/>}</div></main>}
function Team({id,name,logo,language}:{id:number;name:string;logo:string|null;language:"fa"|"en"}){return <div className="flex min-w-[96px] flex-col items-center justify-start gap-2 text-center"><span className="grid h-12 w-12 place-items-center"><TeamCrest name={name} logo={logo}/></span><b className="min-h-8 max-w-24 truncate text-xs leading-4">{teamName(language,id,name)}</b></div>}
