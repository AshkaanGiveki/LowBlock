"use client";
import { AnimatePresence, motion } from "motion/react";
import { ChevronDown, Clock3, Trophy } from "lucide-react";
import { useState } from "react";
import { T, useLanguage } from "@/components/LanguageProvider";
type Match={matchId:string;kickoffAt:string;homeTeam:{name:string;logoUrl:string|null};awayTeam:{name:string;logoUrl:string|null};predictedHome:number;predictedAway:number;actualHome:number|null;actualAway:number|null;points:number|null};
type Player={userId:string;username:string;avatarUrl:string|null;points:number;predictions:Match[]};
function initials(name:string){return name.split(/\s+/).map(x=>x[0]).join("").slice(0,2).toUpperCase()}
export function LeagueRoundLeaderboard({players}:{players:Player[]}){const [open,setOpen]=useState<string|null>(null);const {language}=useLanguage();return <section className="mt-8"><div className="mb-4 flex items-end justify-between"><div><p className="text-xs font-black tracking-[.2em] text-brand"><T fa="جدول امتیازات دور" en="ROUND LEADERBOARD"/></p><h2 className="mt-2 text-2xl font-black"><T fa="رقابت این دور" en="This round's race"/></h2></div><span className="rounded-full border border-white/10 px-3 py-1 text-xs text-[var(--muted)]">{players.length} <T fa="بازیکن" en="players"/></span></div><div className="space-y-3">{players.map((player,index)=>{const expanded=open===player.userId;return <motion.article layout key={player.userId} className={`overflow-hidden rounded-2xl border bg-[#111713] ${expanded?"border-brand/40 shadow-[0_16px_50px_rgba(32,184,121,.1)]":"border-white/[.08]"}`}><button type="button" onClick={()=>setOpen(expanded?null:player.userId)} className="flex w-full items-center gap-3 p-4 text-start"><span className="w-6 text-center text-sm font-black text-[var(--muted)]">{String(index+1).padStart(2,"0")}</span><span className="grid h-11 w-11 shrink-0 place-items-center overflow-hidden rounded-full border border-brand/30 bg-brand/10 text-xs font-black">{player.avatarUrl?<img src={player.avatarUrl} alt="" className="h-full w-full object-cover"/>:initials(player.username)}</span><span className="min-w-0 flex-1"><b className="block truncate">{player.username}</b><small className="text-[11px] text-[var(--muted)]">{player.predictions.length} <T fa="پیش‌بینی" en="predictions"/></small></span><span className="text-end"><b className="block text-xl text-brand">{player.points}</b><small className="text-[10px] text-[var(--muted)]"><T fa="امتیاز دور" en="round points"/></small></span><ChevronDown size={18} className={`text-[var(--muted)] transition-transform ${expanded?"rotate-180 text-brand":""}`}/></button><AnimatePresence initial={false}>{expanded&&<motion.div initial={{height:0,opacity:0}} animate={{height:"auto",opacity:1}} exit={{height:0,opacity:0}} transition={{duration:.28,ease:"easeOut"}}><div className="space-y-2 border-t border-white/[.08] bg-black/15 p-3">{player.predictions.map((match,i)=><motion.div key={match.matchId} initial={{opacity:0,y:8}} animate={{opacity:1,y:0}} transition={{delay:i*.06,duration:.22}} className="rounded-xl border border-white/[.07] bg-[#151c17] p-3"><div className="mb-3 flex items-center justify-between text-[10px] text-[var(--muted)]"><span className="flex items-center gap-1"><Clock3 size={12}/>{new Date(match.kickoffAt).toLocaleDateString(language==="fa"?"fa-IR":"en-GB")} · {new Date(match.kickoffAt).toLocaleTimeString(language==="fa"?"fa-IR":"en-GB",{hour:"2-digit",minute:"2-digit"})}</span><b className="text-brand">{match.points===null?<T fa="در انتظار" en="Pending"/>:`+${match.points}`}</b></div><div className="grid grid-cols-[1fr_auto_1fr] items-center gap-2"><Team team={match.homeTeam}/><span className="rounded-full border border-brand/30 px-2 py-1 text-[9px] font-black tracking-widest text-brand">VS</span><Team team={match.awayTeam}/></div><div className="mt-3 grid grid-cols-2 gap-2 text-center text-xs"><div className="rounded-lg bg-brand/10 p-2"><small className="block text-[10px] text-[var(--muted)]"><T fa="پیش‌بینی کاربر" en="Your prediction"/></small><b className="text-brand">{match.predictedHome} - {match.predictedAway}</b></div><div className="rounded-lg bg-white/5 p-2"><small className="block text-[10px] text-[var(--muted)]"><T fa="نتیجه واقعی" en="Correct result"/></small><b>{match.actualHome===null?"—":`${match.actualHome} - ${match.actualAway}`}</b></div></div></motion.div>)}</div></motion.div>}</AnimatePresence></motion.article>})}{!players.length&&<div className="rounded-2xl border border-dashed border-white/10 p-12 text-center"><T fa="هنوز پیش‌بینی ثبت نشده است" en="No predictions yet"/></div>}</div></section>}
function Team({team}:{team:{name:string;logoUrl:string|null}}){return <div className="flex min-w-0 flex-col items-center gap-1 text-center"><span className="grid h-10 w-10 place-items-center"><img src={team.logoUrl??"/icon.png"} alt="" className="h-8 w-8 object-contain"/></span><span className="line-clamp-1 w-full text-[10px] font-bold text-white/80">{team.name}</span></div>}
