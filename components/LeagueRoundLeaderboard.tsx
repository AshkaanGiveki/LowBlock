"use client";
import {AnimatePresence,motion} from "motion/react";import{ChevronDown,Clock3}from"lucide-react";import{useState}from"react";import{T,useLanguage}from"@/components/LanguageProvider";
type Match={matchId:string;kickoffAt:string;homeTeam:{name:string;logoUrl:string|null};awayTeam:{name:string;logoUrl:string|null};predictedHome:number|null;predictedAway:number|null;actualHome:number|null;actualAway:number|null;points:number|null;revealed:boolean};type Player={userId:string;username:string;avatarUrl:string|null;points:number;predictions:Match[]};
function initials(n:string){return n.split(/\s+/).map(x=>x[0]).join("").slice(0,2).toUpperCase()};function Team({team}:{team:{name:string;logoUrl:string|null}}){return <div className="flex min-w-0 flex-col items-center gap-1 text-center"><img src={team.logoUrl??"/icon.png"} alt="" className="h-8 w-8 object-contain"/><span className="line-clamp-1 w-full text-[10px] font-bold">{team.name}</span></div>}
export function LeagueRoundLeaderboard({players}:{players:Player[]}){const[open,setOpen]=useState<string|null>(null);const{language}=useLanguage();return <section className="mt-8"><div className="mb-4 flex items-end justify-between"><div><p className="text-xs font-black tracking-[.2em] text-brand"><T fa="جدول امتیازات دور" en="ROUND LEADERBOARD"/></p><h2 className="mt-2 text-2xl font-black"><T fa="رقابت این دور" en="This round's race"/></h2></div><span className="rounded-full border border-white/10 px-3 py-1 text-xs text-[var(--muted)]">{players.length} <T fa="بازیکن" en="players"/></span></div><div className="space-y-3">{players.map((p,i)=>{const expanded=open===p.userId;return <motion.article layout key={p.userId} className="overflow-hidden rounded-2xl border border-white/[.08] bg-[#111713]"><button type="button" onClick={()=>setOpen(expanded?null:p.userId)} className="flex w-full items-center gap-3 p-4 text-start"><b className="w-6 text-center text-sm text-[var(--muted)]">{String(i+1).padStart(2,"0")}</b><span className="grid h-11 w-11 shrink-0 place-items-center overflow-hidden rounded-full border border-brand/30 bg-brand/10 text-xs font-black">{p.avatarUrl?<img src={p.avatarUrl} alt="" className="h-full w-full object-cover"/>:initials(p.username)}</span><span className="min-w-0 flex-1"><b className="block truncate">{p.username}</b><small className="text-[11px] text-[var(--muted)]">{p.predictions.length} <T fa="پیش‌بینی" en="predictions"/></small></span><span className="text-end"><b className="block text-xl text-brand">{p.points}</b><small className="text-[10px] text-[var(--muted)]"><T fa="امتیاز دور" en="round points"/></small></span><ChevronDown size={18} className={expanded?"rotate-180 text-brand":"text-[var(--muted)]"}/></button><AnimatePresence initial={false}>{expanded&&<motion.div initial={{height:0,opacity:0}} animate={{height:"auto",opacity:1}} exit={{height:0,opacity:0}}><div className="space-y-2 border-t border-white/[.08] bg-black/15 p-3">{p.predictions.map((m,j)=><motion.div key={m.matchId} initial={{opacity:0,y:8}} animate={{opacity:1,y:0}} transition={{delay:j*.06}} className="rounded-xl border border-white/[.07] bg-[#151c17] p-3"><div className="mb-3 flex items-center justify-between text-[10px] text-[var(--muted)]"><span className="flex items-center gap-1"><Clock3 size={12}/>{new Date(m.kickoffAt).toLocaleDateString(language==="fa"?"fa-IR":"en-GB")} · {new Date(m.kickoffAt).toLocaleTimeString(language==="fa"?"fa-IR":"en-GB",{hour:"2-digit",minute:"2-digit"})}</span><b className="text-brand">{m.points===null?<T fa={m.revealed?"در انتظار نتیجه":"تا شروع بازی مخفی"} en={m.revealed?"Pending":"Hidden until kickoff"}/>:`+${m.points}`}</b></div><div className="grid grid-cols-[1fr_auto_1fr] items-center gap-2"><Team team={m.homeTeam}/><span className="rounded-full border border-brand/30 px-2 py-1 text-[9px] font-black tracking-widest text-brand">VS</span><Team team={m.awayTeam}/></div><div className="mt-3 grid grid-cols-2 gap-2 text-center text-xs"><div className="rounded-lg bg-brand/10 p-2"><small className="block text-[10px] text-[var(--muted)]"><T fa="پیش‌بینی کاربر" en="User prediction"/></small><b className="text-brand">{m.revealed?`${m.predictedHome} - ${m.predictedAway}`:<T fa="مخفی" en="Hidden"/>}</b></div><div className="rounded-lg bg-white/5 p-2"><small className="block text-[10px] text-[var(--muted)]"><T fa="نتیجه واقعی" en="Correct result"/></small><b>{m.actualHome===null?"—":`${m.actualHome} - ${m.actualAway}`}</b></div></div></motion.div>)}</div></motion.div>}</AnimatePresence></motion.article>})}</div></section>}
