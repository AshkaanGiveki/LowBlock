import Link from "next/link";
export function Brand({ variant = "horizontal", className = "" }: { variant?: "horizontal" | "stacked" | "mark"; className?: string }) { if (variant === "mark") return <Link href="/" className={className}><img src="/lowblock.png" alt="LowBlock" className="h-full w-full object-contain"/></Link>; return <Link href="/" className={`${variant === "stacked" ? "flex-col" : "flex-row"} flex items-center justify-center gap-2 ${className}`}><img src="/lowblock.png" alt="" className={variant === "stacked" ? "h-20 w-20 object-contain" : "h-9 w-9 object-contain"}/><img src="/lowblock-typo.png" alt="LowBlock" className={variant === "stacked" ? "h-auto w-44 object-contain" : "h-auto w-28 object-contain"}/></Link>; }
