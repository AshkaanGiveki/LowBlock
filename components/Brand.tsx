import Link from "next/link";
import Image from "next/image";
export function Brand({ variant = "horizontal", className = "" }: { variant?: "horizontal" | "stacked" | "mark"; className?: string }) { if (variant === "mark") return <Link href="/" className={className}><Image src="/lowblock.png" alt="LowBlock" width={80} height={80} className="h-full w-full object-contain"/></Link>; return <Link href="/" className={`${variant === "stacked" ? "flex-col" : "flex-row"} flex items-center justify-center gap-2 ${className}`}><Image src="/lowblock.png" alt="" width={80} height={80} className={variant === "stacked" ? "h-20 w-20 object-contain" : "h-9 w-9 object-contain"}/><Image src="/lowblock-typo.png" alt="LowBlock" width={240} height={60} className={variant === "stacked" ? "h-auto w-44 object-contain" : "h-auto w-28 object-contain"}/></Link>; }
