"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { motion } from "motion/react";
import { BarChart3, Home, LogIn, Trophy, UserRound } from "lucide-react";
const items = [{ href: "/app", label: "خانه", icon: Home }, { href: "/leagues/GB1", label: "لیگ‌ها", icon: Trophy }, { href: "/leaderboard", label: "رتبه‌بندی", icon: BarChart3 }];
export function Nav() { const pathname = usePathname(); const active = (href: string) => href === "/app" ? pathname === "/app" : href === "/leagues/GB1" ? pathname.startsWith("/leagues") : pathname.startsWith(href); return <>
  <aside className="fixed inset-y-0 right-0 z-40 hidden w-72 border-l border-[var(--border)] bg-[#09090c]/90 px-5 py-7 backdrop-blur-2xl md:block"><Link href="/" className="mb-14 flex items-center gap-3 px-3"><span className="grid h-11 w-11 place-items-center rounded-2xl bg-brand text-xl font-black text-white shadow-[0_0_28px_rgba(202,16,63,.35)]">ل</span><span className="text-xl font-black tracking-tight">لوبلاک<span className="text-brand">.</span></span></Link><p className="mb-4 px-3 text-[10px] font-bold uppercase tracking-[.22em] text-[var(--muted)]">فضای پیش‌بینی</p><nav className="space-y-2">{items.map(({ href, label, icon: Icon }) => <Link key={href} href={href} className={`relative flex items-center gap-3 rounded-2xl px-4 py-3.5 text-sm font-bold transition ${active(href) ? "text-white" : "text-[var(--muted)] hover:bg-white/[.04] hover:text-white"}`}>{active(href) && <motion.span layoutId="active-desktop-nav" className="absolute inset-0 rounded-2xl bg-brand/15" transition={{ type: "spring", stiffness: 380, damping: 32 }} />}{active(href) && <motion.span layoutId="active-desktop-line" className="absolute right-0 h-7 w-1 rounded-full bg-brand" /> }<span className="relative z-10 flex items-center gap-3"><Icon size={18}/>{label}</span></Link>)}</nav><div className="absolute bottom-8 left-5 right-5 border-t border-[var(--border)] pt-5"><Link href="/login" className="flex items-center gap-3 rounded-2xl px-4 py-3 text-sm font-bold text-[var(--muted)] transition hover:bg-white/[.04] hover:text-white"><LogIn size={18}/>ورود به حساب</Link></div></aside>
  <header className="fixed inset-x-0 top-0 z-30 hidden h-[76px] border-b border-[var(--border)] bg-[#07070a]/75 px-8 backdrop-blur-2xl md:flex md:items-center md:justify-between md:pr-[21rem]"><div><p className="text-xs text-[var(--muted)]">داشبورد لوبلاک</p><p className="font-bold">فوتبال را دقیق‌تر ببین</p></div><Link href="/signup" className="inline-flex items-center gap-2 rounded-xl bg-brand px-4 py-2.5 text-sm font-black text-white transition hover:bg-brand-hover"><UserRound size={16}/> ساخت حساب</Link></header>
  <nav className="fixed inset-x-4 bottom-4 z-50 flex items-center justify-around rounded-[26px] border border-white/10 bg-[#111116]/90 px-2 py-2 shadow-2xl backdrop-blur-2xl md:hidden">{items.map(({ href, label, icon: Icon }) => <Link key={href} href={href} className={`relative flex min-w-[74px] flex-col items-center gap-1 rounded-2xl px-3 py-2 text-[10px] font-bold ${active(href) ? "text-white" : "text-[var(--muted)]"}`}>{active(href) && <motion.span layoutId="active-mobile-nav" className="absolute inset-0 rounded-2xl bg-brand/20" transition={{ type: "spring", stiffness: 400, damping: 30 }} />}{active(href) && <motion.span layoutId="active-mobile-dot" className="absolute -top-1 h-1 w-5 rounded-full bg-brand" /> }<Icon className="relative z-10" size={19}/><span className="relative z-10">{label}</span></Link>)}<Link href="/login" className="flex min-w-[74px] flex-col items-center gap-1 rounded-2xl px-3 py-2 text-[10px] font-bold text-[var(--muted)]"><LogIn size={19}/><span>حساب</span></Link></nav>
 </>; }
