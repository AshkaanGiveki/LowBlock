"use client";
import { Crown, Medal, Sparkles, Trophy, Users } from "lucide-react";
import { Brand } from "@/components/Brand";
import { useLanguage } from "@/components/LanguageProvider";
type Row={userId:string;points:number;predictions:number;exact:number;username:string;avatarUrl:string|null};
function initials(name:string){return name.trim().split(/\s+/).map((part)=>part[0]).filter(Boolean).slice(0,2).join("");}
export function LeaderboardView({rows,me}:{rows:Row[];me:string|null}){const{t}=useLanguage();const top=rows[0];const totalPicks=rows.reduce((sum,row)=>sum+row.predictions,0);const podium=[rows[1],rows[0],rows[2]].filter(Boolean);return <main className="min-h-screen px-4 pb-28 pt-24 md:px-8 md:pt-32"><div className="mx-auto max-w-6xl"><section className="relative overflow-hidden rounded-3xl border border-white/[.08] bg-[radial-gradient(circle_at_15%_15%,rgba(32,184,121,.22),transparent_35%),linear-gradient(145deg,#13231b,#090d0b)] p-6 md:p-10"><div className="relative"><div className="inline-flex items-center gap-2 rounded-full border border-brand/30 bg-brand/10 px-3 py-1.5 text-xs font-black text-brand"><Crown size={14}/>{t("جدول امتیازات","LEADERBOARD")}</div><h1 className="mt-4 max-w-2xl text-3xl font-black md:text-5xl">{t("رتبه‌بندی بازیکنان پیش‌بینی","Prediction player rankings")}</h1><p className="mt-3 max-w-xl text-sm leading-7 text-[var(--muted)]">{t("امتیاز و عملکرد پیش‌بینی‌کنندگان را در یک نگاه ببینید.","See every predictor’s points and performance at a glance.")}</p><div className="mt-7 grid max-w-2xl grid-cols-3 gap-3"><Metric icon={<Users size={16}/>} value={rows.length} label={t("بازیکن","Players")}/><Metric icon={<Sparkles size={16}/>} value={totalPicks} label={t("پیش‌بینی","Picks")}/><Metric icon={<Trophy size={16}/>} value={top?.points??0} label={t("امتیاز برتر","Top points")}/></div></div><Brand variant="stacked" className="pointer-events-none absolute bottom-8 right-10 hidden opacity-60 md:flex"/></section>{rows.length?<><section className="mt-8 grid items-end gap-3 md:grid-cols-3">{podium.map((row,index)=>{const rank=index===0?2:index===1?1:3;return <div key={row.userId} className={rank===1?"md:order-2 md:pb-8":rank===2?"md:order-1":"md:order-3"}><div className={`rounded-2xl border p-5 text-center ${rank===1?"border-[#e8c66a]/40 bg-[#e8c66a]/10":"border-white/[.08] bg-[#111713]"}`}><div className="mb-3 flex items-center justify-center gap-2 text-xs font-black text-[var(--muted)]"><Medal size={15} className={rank===1?"text-[#e8c66a]":"text-brand"}/>0{rank}</div><Avatar row={row} large/><h2 className="mt-3 truncate font-black">{row.username}</h2><p className="mt-1 text-xs text-[var(--muted)]">{row.predictions} {t("پیش‌بینی","picks")}</p><div className="mt-4 inline-flex items-center gap-2 rounded-lg bg-black/25 px-3 py-2 text-sm font-black text-brand"><Sparkles size={14}/>{row.points}</div></div></div>})}</section><section className="mt-7 overflow-hidden rounded-2xl border border-white/[.08] bg-[#101512]"><div className="grid grid-cols-[42px_1fr_86px] items-center gap-3 border-b border-white/[.07] px-4 py-4 text-[10px] font-black tracking-wide text-[var(--muted)] md:grid-cols-[60px_1fr_150px_100px]"><span>#</span><span>{t("بازیکن","PLAYER")}</span><span className="hidden md:block">{t("عملکرد","PERFORMANCE")}</span><span className="text-left">{t("امتیاز","POINTS")}</span></div>{rows.map((row,index)=>{const accuracy=row.predictions?Math.round((row.exact/row.predictions)*100):0;return <div key={row.userId} className={`grid grid-cols-[42px_1fr_86px] items-center gap-3 border-b border-white/[.05] px-4 py-4 last:border-0 md:grid-cols-[60px_1fr_150px_100px] ${row.userId===me?"bg-brand/10":""}`}><b className={index<3?"text-[#e8c66a]":"text-[var(--muted)]"}>{String(index+1).padStart(2,"0")}</b><div className="flex min-w-0 items-center gap-3"><Avatar row={row}/><div className="min-w-0"><h3 className="truncate text-sm font-black">{row.username}</h3><p className="text-[10px] text-[var(--muted)]">{row.predictions} {t("پیش‌بینی","predictions")} · {row.exact} {t("دقیق","exact")}</p></div></div><div className="hidden md:block"><div className="mb-1 flex justify-between text-[10px] text-[var(--muted)]"><span>{accuracy}%</span><span>{t("دقت","accuracy")}</span></div><div className="h-1.5 overflow-hidden rounded-full bg-white/10"><div className="h-full rounded-full bg-brand" style={{width:`${accuracy}%`}}/></div></div><b className="text-left text-lg text-brand">{row.points}</b></div>})}</section></>:<div className="mt-8 rounded-2xl border border-dashed border-white/10 p-14 text-center"><Trophy className="mx-auto mb-4 text-brand" size={36}/><h2 className="font-black">{t("هنوز بازیکنی ثبت نشده است","No players yet")}</h2><p className="mt-2 text-sm text-[var(--muted)]">{t("اولین پیش‌بینی را ثبت کنید.","Submit the first prediction to get on the board.")}</p></div>}</div></main>}
function Metric({icon,value,label}:{icon:React.ReactNode;value:number;label:string}){return <div className="rounded-xl border border-white/10 bg-black/20 p-3"><div className="flex items-center gap-2 text-brand">{icon}<b className="text-xl text-white">{value}</b></div><p className="mt-1 text-[10px] text-[var(--muted)]">{label}</p></div>}
function Avatar({row,large=false}:{row:Row;large?:boolean}){return <span className={`grid shrink-0 place-items-center overflow-hidden rounded-full border-2 bg-brand/15 font-black ${large?"mx-auto h-16 w-16 border-white/20 text-lg":"h-9 w-9 border-white/10 text-[10px]"}`}>{row.avatarUrl?<img src={row.avatarUrl} alt={row.username} className="h-full w-full object-cover"/>:initials(row.username)}</span>}
