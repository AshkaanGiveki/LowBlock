"use client";
import { Crown, Medal, Sparkles, Trophy, Users } from "lucide-react";
import { Brand } from "@/components/Brand";
import { useLanguage } from "@/components/LanguageProvider";
type Row = { userId:string;points:number;predictions:number;exact:number;username:string;avatarUrl:string|null };
function initials(name:string){const parts=name.trim().split(/\s+/).filter(Boolean);const chars=parts.length>1?[parts[0][0],parts[1][0]]:[...(parts[0]??"ک")].slice(0,2);return chars.join("‌")}
export function LeaderboardView({rows,me}:{rows:Row[];me:string|null}){const{t}=useLanguage();const podium=[rows[1],rows[0],rows[2]].filter(Boolean);return <main className="min-h-screen px-5 pb-28 pt-24 md:mr-72 md:px-10 md:pb-16 md:pt-28"><div className="mx-auto max-w-6xl"><section className="relative overflow-hidden rounded-[34px] border border-white/[.08] bg-[radial-gradient(circle_at_20%_20%,rgba(18,160,105,.3),transparent_34%),linear-gradient(135deg,#101713,#070a08)] p-7 md:p-11"><div className="pointer-events-none absolute -left-20 top-1/2 h-72 w-72 -translate-y-1/2 rounded-full border border-brand/15"/><div className="pointer-events-none absolute -left-8 top-1/2 h-52 w-52 -translate-y-1/2 rounded-full border border-brand/20"/><div className="relative grid items-center gap-8 md:grid-cols-[1fr_240px]"><div><div className="mb-4 inline-flex items-center gap-2 rounded-full border border-[#e8c66a]/30 bg-[#e8c66a]/10 px-3 py-1.5 text-xs font-black text-[#e8c66a]"><Crown size={14}/>{t("لیگ قهرمانان لوبلاک","LOWBLOCK CHAMPIONS")}</div><h1 className="max-w-2xl text-4xl font-black leading-tight md:text-6xl">{t("دقت، تو را به قله می‌رساند.","Accuracy takes you to the top.")}</h1><p className="mt-4 max-w-xl text-sm leading-7 text-[var(--muted)]">{t("هر پیش‌بینی دقیق، یک قدم تا جایگاه اول و نشان طلایی لوبلاک است.","Every exact prediction is one step closer to first place and the LowBlock gold badge.")}</p><div className="mt-7 flex gap-6 text-sm"><span><b className="block text-2xl">{rows.length}</b><small className="text-[var(--muted)]">{t("بازیکن","Players")}</small></span><span><b className="block text-2xl text-brand">{rows[0]?.points??0}</b><small className="text-[var(--muted)]">{t("بیشترین امتیاز","Top score")}</small></span></div></div><Brand variant="stacked" className="hidden opacity-90 md:flex"/></div></section>
{rows.length?<><section className="mt-10 grid items-end gap-3 md:grid-cols-3">{podium.map((row,index)=>{const rank=index===0?2:index===1?1:3;return <div key={row.userId} className={`${rank===1?"md:order-2 md:pb-8":""} ${rank===2?"md:order-1":"md:order-3"}`}><div className={`relative overflow-hidden rounded-[28px] border p-6 text-center ${rank===1?"border-[#e8c66a]/35 bg-gradient-to-b from-[#e8c66a]/15 to-[#111116]":"border-white/[.08] bg-[#111116]"}`}><span className={`absolute left-4 top-4 text-4xl font-black ${rank===1?"text-[#e8c66a]/20":"text-white/10"}`}>0{rank}</span><Avatar row={row} large/><h2 className="mt-4 truncate font-black">{row.username}</h2><p className="mt-1 text-xs text-[var(--muted)]">{row.predictions} {t("پیش‌بینی","predictions")}</p><div className="mt-5 inline-flex items-center gap-2 rounded-full bg-black/25 px-4 py-2 text-sm font-black text-brand"><Sparkles size={14}/>{row.points} {t("امتیاز","pts")}</div></div></div>})}</section><section className="mt-7 overflow-hidden rounded-[28px] border border-white/[.08] bg-[#111116]"><div className="flex items-center justify-between border-b border-white/[.07] px-5 py-4"><div><h2 className="font-black">{t("جدول کامل","Full standings")}</h2><p className="text-[10px] text-[var(--muted)]">{t("به‌روزرسانی پس از ثبت نتایج رسمی","Updated after official results")}</p></div><Users className="text-brand" size={20}/></div>{rows.map((row,index)=><div key={row.userId} className={`grid grid-cols-[42px_1fr_60px] items-center gap-3 border-b border-white/[.05] px-4 py-4 last:border-0 md:grid-cols-[54px_1fr_100px_80px] ${row.userId===me?"bg-brand/10":""}`}><b className={index<3?"text-[#e8c66a]":"text-[var(--muted)]"}>{index+1}</b><div className="flex min-w-0 items-center gap-3"><Avatar row={row}/><div className="min-w-0"><h3 className="truncate text-sm font-black">{row.username}</h3><p className="text-[9px] text-[var(--muted)]">{row.exact} {t("نتیجه دقیق","exact")}</p></div></div><span className="hidden text-xs text-[var(--muted)] md:block">{row.predictions} {t("بازی","picks")}</span><b className="text-left text-brand">{row.points}</b></div>)}</section></>:<div className="mt-8 rounded-[28px] border border-dashed border-white/10 p-14 text-center"><Trophy className="mx-auto mb-4 text-brand" size={36}/><h2 className="font-black">{t("قله هنوز خالی است","The summit is still open")}</h2><p className="mt-2 text-sm text-[var(--muted)]">{t("اولین پیش‌بینی را ثبت کن و صدرنشین شو.","Submit the first prediction and take the lead.")}</p></div>}</div></main>}
function Avatar({row,large=false}:{row:Row;large?:boolean}){return <span className={`mx-auto grid shrink-0 place-items-center overflow-hidden rounded-full border-2 bg-brand/15 font-black ${large?"h-20 w-20 border-white/20 text-lg":"h-10 w-10 border-white/10 text-[10px]"}`}>{row.avatarUrl?<img src={row.avatarUrl} alt={row.username} className="h-full w-full object-cover"/>:initials(row.username)}</span>}
