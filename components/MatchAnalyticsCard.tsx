"use client";
import {useState} from "react";import {PredictionCard} from "@/components/PredictionCard";import {MatchAnalytics} from "@/components/MatchAnalytics";
export function MatchAnalyticsCard({match,initial,index}:{match:React.ComponentProps<typeof PredictionCard>["match"];initial?:{homeGoals:number;awayGoals:number};index:number}){const[open,setOpen]=useState(false);const locked=match.status==="FINISHED"||new Date(match.kickoffAt).getTime()<=Date.now();return <div onClick={e=>{if(locked&&!(e.target instanceof HTMLInputElement||e.target instanceof HTMLButtonElement))setOpen(true)}}><PredictionCard match={match} initial={initial} index={index}/>{open&&<MatchAnalytics matchId={match.providerMatchId} onClose={()=>setOpen(false)}/>}</div>}
