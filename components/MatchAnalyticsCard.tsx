"use client";
import {usePathname} from "next/navigation";import {useState} from "react";import {PredictionCard} from "@/components/PredictionCard";import {MatchAnalytics} from "@/components/MatchAnalytics";import {closeMatchRoute,matchRoute,openMatchRoute} from "@/components/matchNavigation";
export function MatchAnalyticsCard({match,initial,index,openOnMount=false}:{match:React.ComponentProps<typeof PredictionCard>["match"];initial?:{homeGoals:number;awayGoals:number};index:number;openOnMount?:boolean}){const[open,setOpen]=useState(false);const pathname=usePathname();const started=match.status==="LIVE"||match.status==="FINISHED"||match.status==="SUSPENDED"||(match.status==="SCHEDULED"&&new Date(match.kickoffAt).getTime()<=Date.now());const route=matchRoute(match.providerMatchId);return <div onClick={e=>{if(window.innerWidth<768||!started||e.target instanceof HTMLInputElement||e.target instanceof HTMLButtonElement)return;if(pathname!==route)openMatchRoute(match.providerMatchId,pathname);setOpen(true)}}><PredictionCard match={match} initial={initial} index={index} openOnMount={openOnMount}/>{open&&started&&<MatchAnalytics matchId={match.providerMatchId} onClose={()=>{setOpen(false);closeMatchRoute();}}/>}</div>}
