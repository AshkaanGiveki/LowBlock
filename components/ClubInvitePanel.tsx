"use client";

import { useState } from "react";
import { Check, Copy, Link2, RefreshCw, ShieldOff } from "lucide-react";
import { useLanguage } from "@/components/LanguageProvider";

export function ClubInvitePanel({ clubId }: { clubId: string }) { const { t } = useLanguage(); const [link, setLink] = useState(""); const [message, setMessage] = useState(""); const [busy, setBusy] = useState(false); async function create() { setBusy(true); const response = await fetch(`/api/clubs/${clubId}/invitations`, { method: "POST" }); const data = await response.json(); setBusy(false); if (!response.ok) return setMessage(data.error ?? t("ساخت لینک ممکن نیست.", "Could not create link.")); setLink(`${window.location.origin}/invite/${data.inviteToken}`); setMessage(data.reused ? t("لینک دعوت فعلی نمایش داده شد.", "Your active invite link is ready.") : t("لینک دعوت ساخته شد.", "Invite link created.")); } async function revoke() { setBusy(true); const response = await fetch(`/api/clubs/${clubId}/invitations`, { method: "DELETE" }); setBusy(false); if (!response.ok) return setMessage(t("لغو لینک انجام نشد.", "Could not revoke link.")); setLink(""); setMessage(t("لینک دعوت لغو شد.", "Invite link revoked.")); } async function copy() { await navigator.clipboard.writeText(link); setMessage(t("لینک کپی شد.", "Link copied.")); } return <section className="mt-4 rounded-[1.75rem] border border-white/10 bg-[#101812] p-5 sm:p-7"><div className="flex items-center gap-2 text-brand"><Link2 size={18}/><h2 className="font-black">{t("لینک دعوت باشگاه", "Club invite link")}</h2></div><p className="mt-2 text-sm leading-7 text-[var(--muted)]">{t("یک لینک ثابت بسازید و برای هر تعداد بازیکن که می‌خواهید بفرستید. هر زمان لازم بود می‌توانید آن را لغو کنید.", "Create one stable link and share it with as many players as you like. Revoke it whenever you need.")}</p><div className="mt-5 flex flex-wrap gap-2"><button onClick={create} disabled={busy} className="inline-flex items-center gap-2 rounded-xl bg-brand px-4 py-3 text-sm font-black text-[#07100b] disabled:opacity-60"><RefreshCw size={16} className={busy ? "animate-spin" : ""}/>{link ? t("نمایش لینک فعال", "Show active link") : t("ساخت لینک دعوت", "Create invite link")}</button>{link && <><button onClick={copy} className="inline-flex items-center gap-2 rounded-xl border border-brand/30 bg-brand/10 px-4 py-3 text-sm font-black text-brand"><Copy size={16}/>{t("کپی لینک", "Copy link")}</button><button onClick={revoke} disabled={busy} className="inline-flex items-center gap-2 rounded-xl border border-red-400/20 bg-red-400/10 px-4 py-3 text-sm font-black text-red-300"><ShieldOff size={16}/>{t("لغو لینک", "Revoke link")}</button></>}</div>{link && <div className="mt-4 flex items-center gap-2 rounded-xl border border-brand/20 bg-brand/10 p-3 text-xs text-brand"><Check size={15}/><span className="break-all">{link}</span></div>}{message && <p className="mt-3 text-xs text-brand">{message}</p>}</section>; }
