"use client";
import { useEffect, useState } from "react";
import { Trophy } from "lucide-react";
import { useLanguage } from "@/components/LanguageProvider";
type Award = { id: string; competitionName: string; roundNumber: number; seasonStartYear: number; trophyUrl: string };
export function PublicAwardsSection({ username }: { username: string }) { const { t } = useLanguage(); const [awards, setAwards] = useState<Award[]>([]); useEffect(() => { fetch(`/api/profiles/${encodeURIComponent(username)}/awards`).then(r => r.ok ? r.json() : null).then(data => setAwards(data?.awards ?? [])).catch(() => undefined); }, [username]); if (!awards.length) return null; return <section className="mt-4 rounded-[1.75rem] border border-[#e8c66a]/20 bg-[radial-gradient(circle_at_90%_0%,rgba(232,198,106,.14),transparent_38%),linear-gradient(145deg,#152219,#080d0a)] p-5 shadow-[0_18px_60px_rgba(0,0,0,.2)] sm:p-6"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[#e8c66a]/10 text-[#e8c66a]"><Trophy size={19}/></span><div><p className="text-[10px] font-black tracking-[.2em] text-[#e8c66a]">{t("جوایز", "AWARDS")}</p><h2 className="mt-1 text-xl font-black">{t("افتخارات بازیکن", "Player honours")}</h2></div></div><div className="mt-5 grid gap-3 sm:grid-cols-2">{awards.map(award => <div key={award.id} className="flex items-center gap-3 rounded-2xl border border-white/10 bg-black/15 p-3"><img src={award.trophyUrl} alt={`${award.competitionName} Round Winner`} loading="lazy" className="h-20 w-20 object-contain"/><div><b className="block">{award.competitionName}</b><small className="mt-1 block text-white/55">{t("برنده دور", "Round Winner")}</small><small className="mt-1 block text-xs text-white/45">{award.seasonStartYear}/{String(award.seasonStartYear + 1).slice(-2)} · MD{String(award.roundNumber).padStart(2, "0")}</small></div></div>)}</div></section>; }
