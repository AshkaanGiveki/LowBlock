"use client";
import { useEffect, useRef, useState } from "react";
import { Share2, X } from "lucide-react";
import { shareAward } from "@/lib/awards/share";
type Award = { id: string; competitionName: string; roundNumber: number; seasonStartYear: number };
export function AwardReveal() { const [awards, setAwards] = useState<Award[]>([]); const [index, setIndex] = useState(0); const [busy, setBusy] = useState(false); const closeRef = useRef<HTMLButtonElement>(null); useEffect(() => { fetch("/api/awards/unrevealed").then(r => r.ok ? r.json() : null).then(data => { if (data?.awards?.length) setAwards(data.awards); }).catch(() => undefined); }, []); useEffect(() => { if (awards.length) closeRef.current?.focus(); }, [awards.length, index]); if (!awards.length || !awards[index]) return null; const award = awards[index]; async function finish() { await fetch(`/api/awards/${award.id}/reveal`, { method: "PATCH" }).catch(() => undefined); if (index + 1 >= awards.length) setAwards([]); else setIndex(index + 1); } async function share() { setBusy(true); try { await shareAward(`/api/awards/${award.id}/share-image`); } finally { setBusy(false); } } return <div className="award-reveal-backdrop" role="dialog" aria-modal="true" aria-labelledby="award-reveal-title"><div className="award-reveal-panel"><button ref={closeRef} type="button" aria-label="Continue" onClick={finish} className="absolute right-4 top-4 rounded-full p-2 text-white/50 hover:bg-white/10 hover:text-white"><X size={18}/></button><div className="award-reveal-glow"/><img src={`/api/awards/${award.id}/trophy`} alt={`${award.competitionName} Round Winner, ${award.seasonStartYear}/${String(award.seasonStartYear + 1).slice(-2)} Matchday ${award.roundNumber}`} className="award-reveal-trophy"/><p className="award-reveal-kicker">{award.competitionName}</p><h2 id="award-reveal-title">ROUND WINNER</h2><p className="award-reveal-meta">{award.seasonStartYear}/{String(award.seasonStartYear + 1).slice(-2)} · MD{String(award.roundNumber).padStart(2, "0")}</p><p className="award-reveal-scope">LOW BLOCK LEVEL</p><div className="mt-8 flex justify-center gap-3"><button type="button" disabled={busy} onClick={share} className="inline-flex items-center gap-2 rounded-xl bg-brand px-6 py-3 text-sm font-black text-[#06120c]"><Share2 size={16}/>SHARE</button><button type="button" onClick={finish} className="rounded-xl border border-white/15 px-6 py-3 text-sm font-black text-white">CONTINUE</button></div></div></div>; }
