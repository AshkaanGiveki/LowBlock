"use client";
import{useEffect}from"react";import{usePathname,useRouter}from"next/navigation";
const mainPages=["/","/matches","/predictions","/leaderboard","/profile"];
export function SwipeNavigation(){const pathname=usePathname(),router=useRouter();useEffect(()=>{let startX=0,startY=0;const down=(e:TouchEvent)=>{if((e.target as HTMLElement)?.closest("input,textarea,button,[role=dialog]"))return;startX=e.touches[0].clientX;startY=e.touches[0].clientY};const up=(e:TouchEvent)=>{const dx=e.changedTouches[0].clientX-startX,dy=e.changedTouches[0].clientY-startY;if(Math.abs(dx)<75||Math.abs(dx)<Math.abs(dy)*1.35)return;const index=mainPages.indexOf(pathname);if(index>=0){const next=dx<0?index+1:index-1;if(next>=0&&next<mainPages.length)router.push(mainPages[next])}else if(dx>0)router.back()};window.addEventListener("touchstart",down,{passive:true});window.addEventListener("touchend",up,{passive:true});return()=>{window.removeEventListener("touchstart",down);window.removeEventListener("touchend",up)}},[pathname,router]);return null}
