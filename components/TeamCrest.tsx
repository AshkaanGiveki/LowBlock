export function TeamCrest({name,logo,className=""}:{name:string;logo?:string|null;className?:string}){return logo?<img src={logo} alt={name} className={`h-12 w-12 object-contain ${className}`}/>:<span className={`grid h-12 w-12 place-items-center rounded-2xl bg-brand-subtle text-sm font-black text-brand ${className}`}>{name.slice(0,2)}</span>}
