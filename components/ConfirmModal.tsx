"use client";

import { AnimatePresence, motion } from "motion/react";
import { AlertTriangle, X } from "lucide-react";

export function ConfirmModal({ open, title, description, confirmLabel, cancelLabel, onConfirm, onCancel, danger = false }: { open: boolean; title: string; description: string; confirmLabel: string; cancelLabel: string; onConfirm: () => void; onCancel: () => void; danger?: boolean }) {
  return <AnimatePresence>{open && <motion.div className="fixed inset-0 z-[120] grid place-items-center p-5" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}><button type="button" aria-label={cancelLabel} onClick={onCancel} className="absolute inset-0 bg-black/75 backdrop-blur-md"/><motion.section role="dialog" aria-modal="true" initial={{ opacity: 0, y: 16, scale: .94 }} animate={{ opacity: 1, y: 0, scale: 1 }} exit={{ opacity: 0, y: 12, scale: .96 }} transition={{ type: "spring", stiffness: 400, damping: 28 }} className="relative w-full max-w-md overflow-hidden rounded-[1.75rem] border border-white/10 bg-[#111a14] p-6 text-start shadow-[0_28px_100px_rgba(0,0,0,.6)]"><button type="button" onClick={onCancel} aria-label={cancelLabel} className="absolute end-4 top-4 grid h-9 w-9 place-items-center rounded-xl bg-white/[.06] text-white/50 hover:text-white"><X size={17}/></button><span className={`grid h-12 w-12 place-items-center rounded-2xl ${danger ? "bg-red-400/12 text-red-300" : "bg-brand/12 text-brand"}`}><AlertTriangle size={22}/></span><h2 className="mt-5 text-xl font-black text-white">{title}</h2><p className="mt-2 text-sm leading-7 text-white/55">{description}</p><div className="mt-6 flex gap-2"><button type="button" onClick={onCancel} className="flex-1 rounded-xl border border-white/10 bg-white/[.05] px-4 py-3 text-sm font-black text-white/70 hover:bg-white/[.09]">{cancelLabel}</button><button type="button" onClick={onConfirm} className={`flex-1 rounded-xl px-4 py-3 text-sm font-black ${danger ? "bg-red-400 text-[#200b0f]" : "bg-brand text-[#07100b]"}`}>{confirmLabel}</button></div></motion.section></motion.div>}</AnimatePresence>;
}
