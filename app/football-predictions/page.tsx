import type { Metadata } from "next";
import Link from "next/link";
import { LEAGUES } from "@/lib/football/leagues";

export const metadata: Metadata = { title: "Football Predictions", description: "Predict football match scores, compare results, and compete on LowBlock leaderboards.", alternates: { canonical: "/football-predictions" }, openGraph: { title: "Football Predictions | LowBlock", description: "A football prediction game for fans who want to compete across major leagues." } };

export default function FootballPredictionsPage() {
  return <main className="min-h-screen px-4 pb-28 pt-24 md:px-8 md:pt-32"><div className="mx-auto max-w-5xl">
    <section className="rounded-[2rem] border border-white/[.08] bg-[radial-gradient(circle_at_90%_0%,rgba(32,184,121,.24),transparent_38%),linear-gradient(145deg,#14251c,#0a0f0c)] p-7 md:p-12"><p className="text-xs font-black tracking-[.2em] text-brand">LOWBLOCK</p><h1 className="mt-4 max-w-3xl text-4xl font-black leading-tight md:text-6xl">Football predictions with real competition</h1><p className="mt-5 max-w-2xl text-base leading-8 text-white/65">Predict match scores before kick-off, earn points for accurate results, and climb global, league, and club leaderboards.</p><Link href="/matches" className="mt-7 inline-flex rounded-xl bg-brand px-5 py-3 text-sm font-black text-[#07100b]">Predict upcoming matches</Link></section>
    <section className="mt-10 grid gap-5 md:grid-cols-3"><Article title="Predict every score" text="Choose the expected goals for both teams before the prediction deadline. Your picks are saved to your LowBlock account."/><Article title="Compete on leaderboards" text="Compare your performance globally, by league, and with your club throughout the season."/><Article title="Build a record" text="Accurate predictions, weekly performance, and special awards turn every round into a lasting football record."/></section>
    <section className="mt-12"><h2 className="text-2xl font-black">Major league prediction pages</h2><div className="mt-4 grid grid-cols-2 gap-3 md:grid-cols-5">{LEAGUES.map((league) => <Link key={league.code} href={`/leagues/${league.code}`} className="rounded-2xl border border-white/10 bg-white/[.03] p-4 text-sm font-bold transition hover:border-brand/50">{league.enName}<span className="mt-1 block text-xs text-white/45">{league.faName}</span></Link>)}</div></section>
    <section className="mt-12 rounded-2xl border border-white/10 bg-white/[.03] p-6"><h2 className="text-2xl font-black">پیش‌بینی فوتبال در LowBlock</h2><p className="mt-3 leading-8 text-white/65">در LowBlock نتیجه مسابقات فوتبال را پیش‌بینی کنید، امتیاز بگیرید و در جدول‌های جهانی، لیگ‌ها و باشگاه‌ها رقابت کنید. پیش‌بینی هر مسابقه قبل از شروع بازی ثبت می‌شود.</p><div className="mt-5 flex flex-wrap gap-3"><Link href="/how-scoring-works" className="text-sm font-bold text-brand">How scoring works</Link><Link href="/leaderboard" className="text-sm font-bold text-brand">Global leaderboard</Link><Link href="/help" className="text-sm font-bold text-brand">Help center</Link></div></section>
  </div></main>;
}

function Article({ title, text }: { title: string; text: string }) { return <article className="rounded-2xl border border-white/10 bg-white/[.03] p-5"><h2 className="text-lg font-black">{title}</h2><p className="mt-3 text-sm leading-7 text-white/55">{text}</p></article>; }
