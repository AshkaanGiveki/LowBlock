import Link from "next/link"; import { ChevronRight } from "lucide-react"; import { Nav } from "@/components/Nav"; import { getLeague } from "@/lib/football/leagues";
export default async function Round({params}:{params:Promise<{code:string;matchday:string}>}){const {code,matchday}=await params;const league=getLeague(code);return <><Nav/><main className="mx-auto max-w-5xl px-5 pb-20 md:px-10"><Link href={`/leagues/${code}`} className="text-sm text-[var(--muted)]">بازگشت به {league?.faName}</Link><div className="my-8 flex items-end justify-between"><div><p className="text-sm text-brand">{league?.short}</p><h1 className="mt-2 text-3xl font-black">دور {matchday}</h1></div><span className="rounded-full border border-[var(--border)] px-3 py-1.5 text-xs text-[var(--muted)]">بازی‌های واقعی</span></div><div className="panel grid min-h-96 place-items-center p-8 text-center"><div><div className="mx-auto mb-4 grid h-14 w-14 place-items-center rounded-2xl bg-brand-subtle text-2xl text-brand"><ChevronRight/></div><h2 className="text-lg font-bold">این دور هنوز آماده نیست</h2><p className="mt-2 max-w-sm text-sm leading-7 text-[var(--muted)]">پس از همگام‌سازی منبع رسمی، بازی‌های این matchweek همراه با زمان شروع و وضعیت پیش‌بینی اینجا دیده می‌شوند.</p></div></div></main></>}
