import Link from "next/link";
import { notFound } from "next/navigation";
import { ObjectId } from "mongodb";
import { getDb } from "@/lib/db/mongo";
import { getLeague } from "@/lib/football/leagues";
import { currentUserId } from "@/lib/auth/session";
import { LeaderboardView } from "@/components/LeaderboardView";
import { LeagueRoundRail } from "@/components/LeagueRoundRail";
import { T } from "@/components/LanguageProvider";
export const dynamic="force-dynamic";
export default async function LeaguePage({params}:{params:Promise<{code:string}>}){const {code}=await params;const league=getLeague(code);if(!league)notFound();const db=await getDb();const season=await db.collection("matches").find({leagueCode:code},{projection:{seasonStartYear:1}}).sort({seasonStartYear:-1}).limit(1).next();const year=Number(season?.seasonStartYear??new Date().getUTCFullYear());const stats=await db.collection<Record<string,unknown>>("leaderboardStats").find({scope:`LEAGUE:${code}:${year}`}).sort({points:-1,totalPoints:-1}).limit(100).toArray();const ids=stats.map(s=>String(s.userId)).filter(ObjectId.isValid).map(id=>new ObjectId(id));const users=ids.length?await db.collection<{username:string;avatarUrl?:string|null}>("users").find({_id:{$in:ids}},{projection:{username:1,avatarUrl:1}}).toArray():[];const byId=new Map(users.map(u=>[String(u._id),u]));const rows=stats.map(s=>({userId:String(s.userId),points:Number(s.points??0),predictions:Number(s.predictions??0),exact:Number(s.exact??0),username:byId.get(String(s.userId))?.username??"LowBlock Player",avatarUrl:byId.get(String(s.userId))?.avatarUrl??null}));const docs=await db.collection<{matchday:number;status:string;kickoffAt:Date}>("matches").find({leagueCode:code,seasonStartYear:year},{projection:{matchday:1,status:1,kickoffAt:1}}).toArray();const grouped=new Map<number,{number:number;count:number;active:boolean;completed:boolean}>();for(const m of docs){const item=grouped.get(m.matchday)??{number:m.matchday,count:0,active:false,completed:true};item.count++;item.completed=item.completed&&m.status==="FINISHED";item.active=item.active||m.status==="LIVE";grouped.set(m.matchday,item)}const roundList=[...grouped.values()].filter(r=>r.number>0).sort((a,b)=>a.number-b.number);const live=roundList.find(r=>r.active);const upcoming=roundList.find(r=>!r.completed&&!r.active);const activeNumber=live?.number??upcoming?.number??roundList.at(-1)?.number;const rounds=roundList.map(r=>({...r,active:r.number===activeNumber}));return <><main className="min-h-screen px-4 pb-12 pt-24 md:px-8 md:pt-32"><div className="mx-auto max-w-6xl"><section className="relative overflow-hidden rounded-[2rem] border border-white/[.08] bg-[radial-gradient(circle_at_90%_0%,rgba(32,184,121,.24),transparent_38%),linear-gradient(145deg,#14251c,#0a0f0c)] p-6 md:p-9"><div className="flex items-center gap-4"><img src={league.logo} alt="" className="league-logo h-16 w-16 rounded-2xl object-cover"/><div><p className="text-xs font-black tracking-[.2em] text-brand"><T fa={`لیگ · ${year}`} en={`LEAGUE · ${year}`}/></p><h1 className="mt-1 text-3xl font-black"><T fa={league.faName} en={league.enName}/></h1><p className="mt-2 text-sm text-white/60"><T fa="رقابت‌های فصل و جدول امتیازات" en="Season competition and standings"/></p></div></div></section><LeagueRoundRail code={code} rounds={rounds}/><section className="mt-8 flex items-end justify-between"><div><p className="text-xs font-black tracking-[.2em] text-brand"><T fa="جدول کلی لیگ" en="LEAGUE STANDINGS"/></p><h2 className="mt-2 text-2xl font-black"><T fa="برترین پیش‌بینی‌کنندگان" en="Top predictors"/></h2></div><span className="rounded-full border border-white/10 px-3 py-1 text-xs text-[var(--muted)]">{rows.length} <T fa="بازیکن" en="players"/></span></section></div></main><LeaderboardView rows={rows} me={await currentUserId()}/></>}
