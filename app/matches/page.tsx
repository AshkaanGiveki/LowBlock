import { currentUserId } from "@/lib/auth/session";
import { getMatchesPage, getPredictions } from "@/lib/football/data";
import { getTournamentPreferences } from "@/lib/football/preferences";
import { MatchesView } from "@/components/MatchesView";
export const dynamic = "force-dynamic";
export default async function MatchesPage({ searchParams }: { searchParams: Promise<{ match?: string; tab?: string }> }) { const page = await getMatchesPage(30); const userId = await currentUserId(); const [predictions, preferences] = await Promise.all([getPredictions(page.matches.map((match) => match.providerMatchId), userId ?? "guest"), userId ? getTournamentPreferences(userId) : Promise.resolve({ favoriteTournamentIds: [], showAllMatches: true })]); const query = await searchParams; const focused = query.match ? page.matches.find((match) => match.providerMatchId === query.match) : undefined; const started = focused && (focused.status === "LIVE" || focused.status === "FINISHED" || focused.status === "SUSPENDED" || new Date(focused.kickoffAt).getTime() <= Date.now()); const initialTab = query.tab === "started" || (!query.tab && Boolean(started)) ? "started" : "open"; return <MatchesView matches={page.matches} predictions={Object.fromEntries(predictions)} nextCursor={page.nextCursor} hasMore={page.hasMore} initialSelectedLeagues={preferences.favoriteTournamentIds} initialShowAllMatches={preferences.showAllMatches} focusMatchId={query.match} initialTab={initialTab} />; }
