"use client";
import { FormEvent, useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import { Camera, LogOut, Save, UserRound } from "lucide-react";
import { useLanguage } from "@/components/LanguageProvider";

type User = { username: string; avatarUrl: string | null };
export default function ProfilePage() {
  const router = useRouter(); const { t } = useLanguage();
  const [user, setUser] = useState<User | null>(null); const [username, setUsername] = useState(""); const [avatarUrl, setAvatarUrl] = useState(""); const [password, setPassword] = useState(""); const [message, setMessage] = useState(""); const [busy, setBusy] = useState(false);
  useEffect(() => { fetch("/api/auth/me").then((r) => r.json()).then((data) => { if (!data.user) return router.replace("/login"); setUser(data.user); setUsername(data.user.username); setAvatarUrl(data.user.avatarUrl ?? ""); }); }, [router]);
  async function save(event: FormEvent) { event.preventDefault(); setBusy(true); setMessage(""); const res = await fetch("/api/auth/profile", { method: "PUT", headers: { "content-type": "application/json" }, body: JSON.stringify({ username, avatarUrl, password }) }); const data = await res.json().catch(() => ({})); setBusy(false); if (!res.ok) return setMessage(data.error ?? "Could not save your profile"); setPassword(""); setUser(data.user); setMessage("Profile updated"); }
  async function logout() { await fetch("/api/auth/logout", { method: "POST" }); router.push("/"); router.refresh(); }
  return <main className="min-h-screen px-5 pb-28 pt-24 md:px-8 md:pt-32"><div className="mx-auto max-w-2xl"><section className="rounded-2xl border border-white/[.1] bg-[linear-gradient(135deg,#18231e,#0c100e)] p-6 md:p-8"><p className="text-xs font-black tracking-[.18em] text-brand">{t("\u062d\u0633\u0627\u0628 \u06a9\u0627\u0631\u0628\u0631\u06cc", "ACCOUNT SETTINGS")}</p><h1 className="mt-2 text-3xl font-black">{t("\u067e\u0631\u0648\u0641\u0627\u06cc\u0644 \u0634\u0645\u0627", "Your profile")}</h1><p className="mt-2 text-sm text-[var(--muted)]">{t("\u0627\u0637\u0644\u0627\u0639\u0627\u062a \u062d\u0633\u0627\u0628 \u062e\u0648\u062f \u0631\u0627 \u0645\u062f\u06cc\u0631\u06cc\u062a \u06a9\u0646\u06cc\u062f.", "Manage your account details and security.")}</p></section>
    <form onSubmit={save} className="mt-4 rounded-2xl border border-white/[.08] bg-[#101512] p-5 md:p-7"><div className="mb-7 flex items-center gap-4"><div className="grid h-16 w-16 place-items-center overflow-hidden rounded-full border border-brand/30 bg-brand/10 text-brand">{avatarUrl ? <img src={avatarUrl} alt="" className="h-full w-full object-cover" /> : <UserRound size={26}/>}</div><div><b>{user?.username ?? "…"}</b><p className="mt-1 text-xs text-[var(--muted)]">{t("\u0639\u06a9\u0633 \u067e\u0631\u0648\u0641\u0627\u06cc\u0644 \u06cc\u0627 \u062d\u0631\u0648\u0641 \u0627\u0648\u0644 \u0646\u0627\u0645", "Profile image or initials")}</p></div></div>
      <label className="block text-sm font-bold">{t("\u0646\u0627\u0645 \u06a9\u0627\u0631\u0628\u0631\u06cc", "Username")}<input required minLength={3} maxLength={24} value={username} onChange={(e) => setUsername(e.target.value)} className="mt-2 w-full rounded-lg border border-white/10 bg-black/20 px-3.5 py-3 text-white" /></label>
      <label className="mt-4 block text-sm font-bold"><span className="flex items-center gap-2"><Camera size={15}/>{t("\u0646\u0634\u0627\u0646\u06cc \u0639\u06a9\u0633 \u067e\u067e\u0631\u0648\u0641\u0627\u06cc\u0644", "Profile image URL")}</span><input type="url" value={avatarUrl} onChange={(e) => setAvatarUrl(e.target.value)} placeholder="https://…" className="mt-2 w-full rounded-lg border border-white/10 bg-black/20 px-3.5 py-3 text-white" /></label>
      <label className="mt-4 block text-sm font-bold">{t("\u0631\u0645\u0632 \u0639\u0628\u0648\u0631 \u062c\u062f\u06cc\u062f", "New password")}<input type="password" minLength={8} value={password} onChange={(e) => setPassword(e.target.value)} placeholder={t("\u0628\u0631\u0627\u06cc \u0628\u062f\u0648\u0646 \u062a\u063a\u06cc\u06cc\u0631 \u06af\u0630\u0627\u0631\u06cc\u062f", "Leave blank to keep current")} className="mt-2 w-full rounded-lg border border-white/10 bg-black/20 px-3.5 py-3 text-white" /></label>
      {message && <p className="mt-4 rounded-lg bg-brand/10 p-3 text-sm text-brand">{message}</p>}<div className="mt-6 flex flex-wrap gap-3"><button disabled={busy} className="inline-flex items-center gap-2 rounded-lg bg-brand px-4 py-3 text-sm font-black text-white"><Save size={16}/>{busy ? "…" : t("\u0630\u062e\u06cc\u0631\u0647 \u062a\u063a\u06cc\u06cc\u0631\u0627\u062a", "Save changes")}</button><button type="button" onClick={logout} className="inline-flex items-center gap-2 rounded-lg border border-red-400/30 px-4 py-3 text-sm font-black text-red-300"><LogOut size={16}/>{t("\u062e\u0631\u0648\u062c", "Log out")}</button></div>
    </form></div></main>;
}
