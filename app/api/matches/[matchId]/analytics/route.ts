import { NextResponse } from "next/server";
import { ObjectId } from "mongodb";
import { getDb } from "@/lib/db/mongo";
export const dynamic="force-dynamic";
export async function GET(_req:Request,{params}:{params:Promise<{matchId:string}>}){const {matchId}=await params;const db=await getDb();const match=await db.collection<any>("matches").findOne({provider:"football-api",providerMatchId:matchId});if(!match)return NextResponse.json({error:"match not found"},{status:404});if(new Date(match.kickoffAt).getTime()>Date.now())return NextResponse.json({error:"analytics unavailable"},{status:403});const predictions=await db.collection<any>("predictions").find({matchId,userId:{$ne:"guest"}}).toArray();const ids=predictions.map(p=>p.userId).filter((id:string)=>ObjectId.isValid(id)).map((id:string)=>new ObjectId(id));const users=ids.length?await db.collection<any>("users").find({_id:{$in:ids}},{projection:{username:1,avatarUrl:1}}).toArray():[];const names=new Map(users.map(u=>[String(u._id),u]));const distribution=new Map<string,number>();let totalHome=0,totalAway=0;for(const p of predictions){const key=`${p.homeGoals}-${p.awayGoals}`;distribution.set(key,(distribution.get(key)??0)+1);totalHome+=p.homeGoals;totalAway+=p.awayGoals}return NextResponse.json({match:{homeTeam:match.homeTeam,awayTeam:match.awayTeam,homeGoals:match.homeGoals,awayGoals:match.awayGoals},total:predictions.length,average:{home:predictions.length?totalHome/predictions.length:0,away:predictions.length?totalAway/predictions.length:0},distribution:[...distribution.entries()].map(([score,count])=>({score,count})).sort((a,b)=>b.count-a.count),users:predictions.map(p=>({username:names.get(p.userId)?.username??"LowBlock Player",avatarUrl:names.get(p.userId)?.avatarUrl??null,homeGoals:p.homeGoals,awayGoals:p.awayGoals}))});}
