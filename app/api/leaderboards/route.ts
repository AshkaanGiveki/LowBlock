import { NextResponse } from "next/server";
import { getDb } from "@/lib/db/mongo";
import { getCanonicalLeaderboard } from "@/lib/domain/leaderboards";
export async function GET(req: Request) { const url = new URL(req.url); const year = url.searchParams.get("seasonStartYear"); const leagueCode = url.searchParams.get("leagueCode"); const clubId = url.searchParams.get("clubId"); const weekly = url.searchParams.get("weekly") === "true"; const weeklyOffset = url.searchParams.get("week") === "previous" ? 1 : 0; const limit = Number(url.searchParams.get("limit") ?? 40); const encoded = url.searchParams.get("cursor"); let cursor; if (encoded) { try { cursor = JSON.parse(Buffer.from(encoded, "base64url").toString("utf8")); } catch { return NextResponse.json({ error: "Invalid cursor" }, { status: 400 }); } } const rows = await getCanonicalLeaderboard(await getDb(), { seasonStartYear: weekly ? null : year ? Number(year) : null, leagueCode, clubId, weekly, weeklyOffset }, Math.min(Math.max(limit, 1), 50), cursor); const next = rows.at(-1)?.cursor; return NextResponse.json({ rows, nextCursor: next ? Buffer.from(JSON.stringify(next)).toString("base64url") : null }); }
