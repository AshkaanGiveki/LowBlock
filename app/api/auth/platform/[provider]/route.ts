import { NextResponse } from "next/server";
import { createSession } from "@/lib/auth/session";
import { makePendingPlatformCookie, verifyInitData, type Provider } from "@/lib/platform/identity";
function providerOf(value:string):Provider|null{return value.toLowerCase()==="telegram"?"TELEGRAM":value.toLowerCase()==="bale"?"BALE":null}
export async function POST(req:Request,{params}:{params:Promise<{provider:string}>}){const provider=providerOf((await params).provider);if(!provider)return NextResponse.json({error:"PLATFORM_AUTH_INVALID"},{status:404});const body=await req.json().catch(()=>null) as {initData?:unknown}|null;try{const user=verifyInitData(typeof body?.initData==="string"?body.initData:"",provider);const linked=await getIdentityByPlatform(provider,user.id);if(linked){const session=await createSession(String(linked.userId));const response=NextResponse.json({ok:true,linked:true});response.cookies.set(session.name,session.value,{httpOnly:true,secure:process.env.NODE_ENV==="production",sameSite:"lax",path:"/",maxAge:session.maxAge});return response}const response=NextResponse.json({ok:false,error:"LINK_REQUIRED",provider});response.cookies.set("lowblock_platform_pending",makePendingPlatformCookie(user,provider),{httpOnly:true,secure:process.env.NODE_ENV==="production",sameSite:"lax",path:"/",maxAge:600});return response}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"PLATFORM_AUTH_INVALID"},{status:401})}}
async function getIdentityByPlatform(provider:Provider,id:string){const {getDb}=await import("@/lib/db/mongo");return getDb().then(db=>db.collection<{userId:string}>("externalIdentities").findOne({provider,providerUserId:id},{projection:{userId:1}}))}
