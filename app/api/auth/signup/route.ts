import { NextResponse } from "next/server"; import { z } from "zod"; import { getDb } from "@/lib/db/mongo"; import { hashPassword } from "@/lib/auth/password"; import { isValidUsername, normalizeUsername } from "@/lib/auth/username";
const body=z.object({username:z.string().min(3).max(24),password:z.string().min(8).max(128)});
export async function POST(req:Request){try{const parsed=body.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:"اطلاعات واردشده معتبر نیست"},{status:400});const username=parsed.data.username.trim();if(!isValidUsername(username))return NextResponse.json({error:"نام کاربری معتبر نیست"},{status:400});const db=await getDb();const now=new Date();await db.collection("users").insertOne({username,normalizedUsername:normalizeUsername(username),passwordHash:await hashPassword(parsed.data.password),createdAt:now,updatedAt:now});return NextResponse.json({ok:true},{status:201});}catch(e){if(e&&typeof e==='object'&&'code' in e&&(e as {code?:number}).code===11000)return NextResponse.json({error:"این نام کاربری قبلاً ثبت شده است"},{status:409});return NextResponse.json({error:"ثبت‌نام موقتاً در دسترس نیست"},{status:503});}}
