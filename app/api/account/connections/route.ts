import { NextResponse } from "next/server"; import { ObjectId } from "mongodb"; import { currentUserId } from "@/lib/auth/session"; import { getDb } from "@/lib/db/mongo"; import { getIdentity, type Provider } from "@/lib/platform/identity";
export async function GET(){const userId=await currentUserId();if(!userId||!ObjectId.isValid(userId))return NextResponse.json({error:"AUTH_REQUIRED"},{status:401});const [telegram,bale]=await Promise.all([getIdentity(userId,"TELEGRAM"),getIdentity(userId,"BALE")]);return NextResponse.json({telegram,bale})}
export async function DELETE(req:Request){const userId=await currentUserId();if(!userId)return NextResponse.json({error:"AUTH_REQUIRED"},{status:401});const provider=(await req.json().catch(()=>null) as {provider?:string}|null)?.provider?.toUpperCase() as Provider;if(provider!=="TELEGRAM"&&provider!=="BALE")return NextResponse.json({error:"INVALID_PROVIDER"},{status:400});await getDb().then(db=>db.collection("externalIdentities").deleteOne({userId,provider}));return NextResponse.json({ok:true})}
