import { NextResponse } from "next/server"; import { currentUserId } from "@/lib/auth/session"; import { createLinkToken, type Provider } from "@/lib/platform/identity"; import { env } from "@/lib/env";
export async function POST(_req:Request,{params}:{params:Promise<{provider:string}>}){const userId=await currentUserId();if(!userId)return NextResponse.json({error:"AUTH_REQUIRED"},{status:401});const provider=(await params).provider.toUpperCase() as Provider;if(provider!=="TELEGRAM"&&provider!=="BALE")return NextResponse.json({error:"INVALID_PROVIDER"},{status:400});const token=await createLinkToken(userId,provider);const username=provider==="TELEGRAM"?env.TELEGRAM_BOT_USERNAME:env.BALE_BOT_USERNAME;if(!username)return NextResponse.json({error:"PLATFORM_NOT_CONFIGURED"},{status:503});const url=provider==="TELEGRAM"?`https://t.me/${encodeURIComponent(username)}?start=${encodeURIComponent(token)}`:`https://ble.ir/${encodeURIComponent(username)}?start=${encodeURIComponent(token)}`;return NextResponse.json({url})}
