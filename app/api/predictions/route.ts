import { NextResponse } from "next/server"; import { z } from "zod"; import { getDb } from "@/lib/db/mongo"; import { currentUserId } from "@/lib/auth/session"; import { isPredictionLocked } from "@/lib/domain/predictionLock";
const body=z.object({matchId:z.string().min(1),homeGoals:z.number().int().min(0).max(30),awayGoals:z.number().int().min(0).max(30)});
export async function POST(req:Request){const userId=await currentUserId();if(!userId)return NextResponse.json({error:"AUTH_REQUIRED"},{status:401});const parsed=body.safeParse(await req.json().catch(()=>null)); if(!parsed.success)return NextResponse.json({error:"INVALID_PREDICTION"},{status:400}); const db=await getDb(); const match=await db.collection<any>("matches").findOne({provider:"football-api",providerMatchId:parsed.data.matchId,rawApiResponse:{$exists:true}}); if(!match)return NextResponse.json({error:"MATCH_NOT_FOUND"},{status:404}); if(isPredictionLocked(match))return NextResponse.json({error:"PREDICTION_LOCKED"},{status:409}); const now=new Date(); await db.collection("predictions").updateOne({userId,matchId:parsed.data.matchId},{$set:{...parsed.data,userId,updatedAt:now},$setOnInsert:{createdAt:now}},{upsert:true}); return NextResponse.json({ok:true});}
