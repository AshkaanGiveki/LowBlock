import { NextResponse } from "next/server";
import { getDb } from "@/lib/db/mongo";
export const dynamic = "force-dynamic";
export async function GET(req: Request, { params }: { params: Promise<{ username: string }> }) { const { username } = await params; const url = new URL(req.url); const limit = Math.min(Math.max(Number(url.searchParams.get("limit") ?? 20), 1), 50); const cursor = url.searchParams.get("cursor"); const db = await getDb(); const user = await db.collection<any>("users").findOne({ normalizedUsername: username.toLowerCase() }, { projection: { _id: 1 } }); if (!user) return NextResponse.json({ error: "Profile not found" }, { status: 404 }); const predictionIds = (await db.collection("predictions").find({ userId: String(user._id) }, { projection: { matchId: 1 } }).toArray()).map(row => row.matchId); const now = new Date(); const matchQuery: any = { providerMatchId: { $in: predictionIds }, $or: [{ status: { $in: ["LIVE", "FINISHED", "SUSPENDED"] } }, { status: "SCHEDULED", kickoffAt: { $lte: now } }] }; if (cursor) matchQuery.kickoffAt = { $lt: new Date(cursor), $lte: now }; const matches = await db.collection<any>("matches").find(matchQuery).sort({ kickoffAt: -1 }).limit(limit).toArray(); const ids = matches.map(match => match.providerMatchId); const predictions = await db.collection<any>("predictions").find({ userId: String(user._id), matchId: { $in: ids } }).toArray(); const scores = await db.collection<any>("predictionScores").find({ userId: String(user._id), matchId: { $in: ids } }).toArray(); const predictionById = new Map(predictions.map(prediction => [prediction.matchId, prediction])); const scoreById = new Map(scores.map(score => [score.matchId, score])); const rows = matches.map(match => { const prediction = predictionById.get(match.providerMatchId); const score = scoreById.get(match.providerMatchId); return { matchId: match.providerMatchId, kickoffAt: new Date(match.kickoffAt).toISOString(), leagueCode: match.leagueCode, matchday: match.matchday, homeTeam: match.homeTeam, awayTeam: match.awayTeam, predictedHome: prediction?.homeGoals ?? null, predictedAway: prediction?.awayGoals ?? null, actualHome: match.homeGoals, actualAway: match.awayGoals, points: Number(score?.points ?? 0), exact: Boolean(score?.exactScore) }; }); return NextResponse.json({ rows, nextCursor: rows.at(-1)?.kickoffAt ?? null }); }
