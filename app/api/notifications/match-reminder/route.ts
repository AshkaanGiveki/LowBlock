import { NextResponse } from "next/server"; import { z } from "zod"; import { verifyQstashSignature } from "@/lib/notifications/qstash"; import { processMatchReminder } from "@/lib/notifications/reminders";
export async function POST(req:Request){const body=await req.text();if(!await verifyQstashSignature(body,req.headers.get("Upstash-Signature"),req.url))return NextResponse.json({error:"unauthorized"},{status:401});let json:unknown;try{json=JSON.parse(body)}catch{return NextResponse.json({error:"invalid payload"},{status:400})}const parsed=z.object({matchId:z.string().min(1)}).safeParse(json);if(!parsed.success)return NextResponse.json({error:"invalid payload"},{status:400});return NextResponse.json({ok:true,...await processMatchReminder(parsed.data.matchId)})}
