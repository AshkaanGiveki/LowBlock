import { NextResponse } from "next/server"; import { currentUserId } from "@/lib/auth/session"; import { getDb } from "@/lib/db/mongo";
export async function GET(){const userId=await currentUserId();if(!userId)return NextResponse.json({error:"AUTH_REQUIRED"},{status:401});const p=await getDb().then(db=>db.collection<any>("notificationPreferences").findOne({userId}));return NextResponse.json({enabled:p?.telegramPredictionRemindersEnabled===true})}
export async function PUT(req:Request){const userId=await currentUserId();if(!userId)return NextResponse.json({error:"AUTH_REQUIRED"},{status:401});const enabled=((await req.json().catch(()=>null)) as {enabled?:unknown}|null)?.enabled===true;const identity=await getDb().then(db=>db.collection("externalIdentities").findOne({userId,provider:"TELEGRAM"}));if(enabled&&!identity)return NextResponse.json({error:"TELEGRAM_NOT_CONNECTED"},{status:409});await getDb().then(db=>db.collection("notificationPreferences").updateOne({userId},{$set:{userId,telegramPredictionRemindersEnabled:enabled,updatedAt:new Date()},$setOnInsert:{createdAt:new Date()}},{upsert:true}));return NextResponse.json({ok:true,enabled})}
