"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { useLanguage } from "@/components/LanguageProvider";
export default function CreateClubPage() { const router = useRouter(); const { t } = useLanguage(); const [name, setName] = useState(""); const [mode, setMode] = useState<"INVITE_ONLY" | "RECRUITING">("INVITE_ONLY"); const [error, setError] = useState(""); const [busy, setBusy] = useState(false); async function submit(e: FormEvent) { e.preventDefault(); setBusy(true); const response = await fetch("/api/clubs", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ name, discoveryMode: mode }) }); const data = await response.json(); if (!response.ok) { setError(data.error ?? "Could not create Club"); setBusy(false); return; } router.replace("/club"); } return <main className="min-h-screen px-5 pb-28 pt-24 md:px-8 md:pt-32"><form onSubmit={submit} className="mx-auto max-w-xl rounded-3xl border border-white/10 bg-[#101512] p-7"><p className="text-xs font-black tracking-[.2em] text-brand">{t("ساخت باشگاه", "CREATE CLUB")}</p><h1 className="mt-2 text-3xl font-black">{t("باشگاه خود را بسازید", "Create your Club")}</h1><label className="mt-7 block text-sm font-bold">{t("نام باشگاه", "Club name")}<input required value={name} onChange={e => setName(e.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3" /></label><fieldset className="mt-6 space-y-3"><legend className="text-sm font-bold">{t("چگونه بازیکن پیدا می‌کنید؟", "How will you find players?")}</legend>{[["INVITE_ONLY",t("دعوت از دوستان", "Invite people myself")],["RECRUITING",t("جذب از LowBlock", "Recruit from LowBlock")]].map(([value,label]) => <label key={value} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><input type="radio" checked={mode === value} onChange={() => setMode(value as typeof mode)} />{label}</label>)}</fieldset>{error && <p className="mt-4 rounded-lg bg-red-950/30 p-3 text-sm text-red-200">{error}</p>}<button disabled={busy} className="mt-7 w-full rounded-xl bg-brand px-5 py-3 font-black">{busy ? "…" : t("ساخت باشگاه", "Create Club")}</button></form></main>; }

