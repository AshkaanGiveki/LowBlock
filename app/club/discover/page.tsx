import Link from "next/link";
import { redirect } from "next/navigation";
import { currentUserId } from "@/lib/auth/session";
import { getDb } from "@/lib/db/mongo";
export const dynamic = "force-dynamic";
export default async function DiscoverClubsPage() { const userId = await currentUserId(); const db = await getDb(); if (userId && await db.collection("clubJoinRequests").findOne({ userId, status: "PENDING" })) redirect("/club"); const clubs = await db.collection<any>("clubs").find({ discoveryMode: "RECRUITING", visibility: "PUBLIC" }).sort({ state: 1, createdAt: -1 }).limit(50).toArray(); return <main className="min-h-screen px-5 pb-28 pt-24 md:px-8 md:pt-32"><div className="mx-auto max-w-3xl"><p className="text-xs font-black tracking-[.2em] text-brand">FIND A CLUB</p><h1 className="mt-2 text-3xl font-black">Recruiting Clubs</h1><div className="mt-6 space-y-3">{clubs.length ? clubs.map(club => <Link key={String(club._id)} href={`/club/${club._id}/join`} className="flex items-center justify-between rounded-2xl border border-white/10 bg-[#101512] p-5"><span><b className="block text-lg">{club.name}</b><small className="text-[var(--muted)]">{club.state === "ACTIVE" ? "Active" : "Forming"}</small></span><span className="rounded-lg border border-brand/30 px-3 py-2 text-xs font-black text-brand">Request to join</span></Link>) : <div className="rounded-2xl border border-dashed border-white/15 p-12 text-center text-[var(--muted)]">No recruiting Clubs yet.</div>}</div></div></main>; }
