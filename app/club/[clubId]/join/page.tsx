"use client";
import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import { ArrowLeft, Send } from "lucide-react";
import { useLanguage } from "@/components/LanguageProvider";
export default function RequestJoin({ params }: { params: Promise<{ clubId: string }> }) { const router = useRouter(); const { t } = useLanguage(); const [clubId, setClubId] = useState(""); const [message, setMessage] = useState(""); useEffect(() => { params.then(({ clubId: id }) => setClubId(id)); }, [params]); async function join() { const response = await fetch(`/api/clubs/${clubId}/join`, { method: "POST" }); const data = await response.json(); if (!response.ok) return setMessage(data.error ?? "Could not send request"); setMessage(t("درخواست ارسال شد.", "Request sent.")); setTimeout(() => router.replace("/club"), 900); } return <main className="min-h-screen px-5 pb-28 pt-24 md:px-8 md:pt-32"><div className="mx-auto max-w-xl"><button onClick={() => router.back()} className="inline-flex items-center gap-2 text-sm text-[var(--muted)]"><ArrowLeft size={16}/>Back</button><section className="mt-5 rounded-3xl border border-white/10 bg-[radial-gradient(circle_at_100%_0%,rgba(32,184,121,.2),transparent_40%),linear-gradient(145deg,#14251c,#0a0f0c)] p-7 text-center"><Send className="mx-auto text-brand" size={28}/><h1 className="mt-4 text-3xl font-black">{t("درخواست عضویت", "Request to join")}</h1><p className="mt-3 text-sm leading-7 text-[var(--muted)]">{t("مالک باشگاه درخواست شما را بررسی می‌کند. تا زمان پذیرش، باشگاه فعلی شما تغییر نمی‌کند.", "The Club owner will review your request. Your current Club will not change until it is accepted and confirmed.")}</p><button onClick={join} className="mt-7 w-full rounded-xl bg-brand px-5 py-3 font-black">{t("ارسال درخواست", "Send request")}</button>{message && <p className="mt-4 rounded-xl bg-brand/10 p-3 text-sm text-brand">{message}</p>}</section></div></main>; }
