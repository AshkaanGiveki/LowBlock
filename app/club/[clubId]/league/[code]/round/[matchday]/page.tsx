import { notFound } from "next/navigation";
import { ObjectId } from "mongodb";
import { getDb } from "@/lib/db/mongo";
import { getLeague } from "@/lib/football/leagues";
import { getCanonicalLeaderboard } from "@/lib/domain/leaderboards";
export const dynamic = "force-dynamic";
export default async function ClubRoundPage({ params }: { params: Promise<{ clubId: string; code: string; matchday: string }> }) { const { clubId, code, matchday } = await params; const league = getLeague(code); const day = Number(matchday); if (!league || !ObjectId.isValid(clubId) || !Number.isInteger(day)) notFound(); const db = await getDb(); const club = await db.collection<any>("clubs").findOne({ _id: new ObjectId(clubId) }); const matches = await db.collection<any>("matches").find({ leagueCode: code, matchday: day }).sort({ kickoffAt: 1 }).toArray(); if (!club || !matches.length) notFound(); const year = Number(matches[0].seasonStartYear); const rows = await getCanonicalLeaderboard(db, { clubId, leagueCode: code, seasonStartYear: year, matchday: day }, 100); return <main className="min-h-screen px-4 pb-28 pt-24 md:px-8 md:pt-32"><div className="mx-auto max-w-4xl"><section className="rounded-3xl border border-white/10 bg-[linear-gradient(145deg,#14251c,#0a0f0c)] p-7"><p className="text-xs font-black tracking-[.2em] text-brand">{club.name} · {league.enName}</p><h1 className="mt-2 text-3xl font-black">Round {day}</h1></section><section className="mt-5 space-y-3">{matches.map(match => <div key={match.providerMatchId} className="rounded-2xl border border-white/10 bg-[#101512] p-5"><div className="flex items-center justify-between"><span>{match.homeTeam.name} · {match.awayTeam.name}</span><span className="text-xs text-[var(--muted)]">{match.status}</span></div><div className="mt-3 text-center text-lg font-black">{match.homeGoals == null ? "—" : `${match.homeGoals} - ${match.awayGoals}`}</div></div>)}</section><section className="mt-8 rounded-3xl border border-white/10 bg-[#101512] p-5"><h2 className="text-2xl font-black">Round leaderboard</h2><div className="mt-4 space-y-2">{rows.map(row => <div key={row.userId} className="flex justify-between rounded-xl bg-black/20 p-3"><span>{row.rank}. {row.username}</span><b className="text-brand">{row.points}</b></div>)}</div></section></div></main>; }
