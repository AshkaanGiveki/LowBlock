import { notFound } from "next/navigation";
import { ObjectId } from "mongodb";
import { getDb } from "@/lib/db/mongo";
import { getLeague } from "@/lib/football/leagues";
import { getCanonicalLeaderboard } from "@/lib/domain/leaderboards";
import { ClubRoundFixtures } from "@/components/ClubRoundFixtures";
export const dynamic = "force-dynamic";
export default async function ClubRoundPage({ params }: { params: Promise<{ clubId: string; code: string; matchday: string }> }) { const { clubId, code, matchday } = await params; const league = getLeague(code); const day = Number(matchday); if (!league || !ObjectId.isValid(clubId) || !Number.isInteger(day)) notFound(); const db = await getDb(); const club = await db.collection<any>("clubs").findOne({ _id: new ObjectId(clubId) }); const matches = await db.collection<any>("matches").find({ leagueCode: code, matchday: day }).sort({ kickoffAt: 1 }).toArray(); if (!club || !matches.length) notFound(); const year = Number(matches[0].seasonStartYear); const rows = await getCanonicalLeaderboard(db, { clubId, leagueCode: code, seasonStartYear: year, matchday: day }, 100); return <main className="min-h-screen px-4 pb-28 pt-24 md:px-8 md:pt-32"><div className="mx-auto max-w-4xl"><section className="relative overflow-hidden rounded-3xl border border-white/10 bg-[radial-gradient(circle_at_90%_0%,rgba(32,184,121,.22),transparent_40%),linear-gradient(145deg,#14251c,#0a0f0c)] p-7"><p className="text-xs font-black tracking-[.2em] text-brand">{club.name} · {league.enName}</p><h1 className="mt-2 text-3xl font-black">Round {day}</h1><p className="mt-2 text-sm text-white/60">{matches.every(match => match.status === "FINISHED") ? "FINAL" : matches.some(match => match.status === "LIVE") ? "LIVE" : "IN PROGRESS"}</p></section><ClubRoundFixtures clubId={clubId} fixtures={matches.map(match => ({ providerMatchId: match.providerMatchId, kickoffAt: new Date(match.kickoffAt).toISOString(), status: match.status, homeGoals: match.homeGoals, awayGoals: match.awayGoals, homeTeam: match.homeTeam, awayTeam: match.awayTeam }))}/><section className="mt-8 rounded-3xl border border-white/10 bg-[#101512] p-5"><div className="flex items-end justify-between"><div><p className="text-xs font-black tracking-[.16em] text-brand">ROUND STANDINGS</p><h2 className="mt-1 text-2xl font-black">{league.enName}</h2></div><span className="text-xs text-[var(--muted)]">{rows.length} players</span></div><div className="mt-4 space-y-2">{rows.map(row => <div key={row.userId} className="flex items-center justify-between rounded-xl bg-black/20 p-3"><span>{row.rank}. {row.username}</span><b className="text-brand">{row.points}</b></div>)}</div></section></div></main>; }
