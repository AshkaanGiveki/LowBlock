import { notFound } from "next/navigation";
import { ObjectId } from "mongodb";
import { getDb } from "@/lib/db/mongo";
import { getLeague } from "@/lib/football/leagues";
import { getCanonicalLeaderboard } from "@/lib/domain/leaderboards";
import { LeagueRoundRail } from "@/components/LeagueRoundRail";
export const dynamic = "force-dynamic";
export default async function ClubLeaguePage({ params }: { params: Promise<{ clubId: string; code: string }> }) { const { clubId, code } = await params; const league = getLeague(code); if (!league || !ObjectId.isValid(clubId)) notFound(); const db = await getDb(); const club = await db.collection<any>("clubs").findOne({ _id: new ObjectId(clubId) }); if (!club) notFound(); const year = Number((await db.collection("matches").findOne({ leagueCode: code }, { sort: { seasonStartYear: -1 }, projection: { seasonStartYear: 1 } }))?.seasonStartYear ?? new Date().getUTCFullYear()); const rows = await getCanonicalLeaderboard(db, { clubId, leagueCode: code, seasonStartYear: year }, 100); const rounds = await db.collection<any>("rounds").find({ leagueCode: code, seasonId: String(year) }).sort({ number: 1 }).toArray(); return <main className="min-h-screen px-4 pb-28 pt-24 md:px-8 md:pt-32"><div className="mx-auto max-w-5xl"><section className="rounded-3xl border border-white/10 bg-[linear-gradient(145deg,#14251c,#0a0f0c)] p-7"><p className="text-xs font-black tracking-[.2em] text-brand">{club.name}</p><h1 className="mt-2 text-3xl font-black">{league.enName} · {year}</h1></section><LeagueRoundRail code={code} basePath={`/club/${clubId}/league/${code}`} rounds={rounds.map(round => ({ number: round.number, count: round.eligibleFixtures, active: round.status === "LIVE", completed: round.status === "FINAL" }))}/><section className="mt-8 rounded-3xl border border-white/10 bg-[#101512] p-5"><h2 className="text-2xl font-black">{league.enName} standings</h2><div className="mt-4 space-y-2">{rows.map(row => <div key={row.userId} className="flex items-center justify-between rounded-xl bg-black/20 p-3"><span>{row.rank}. {row.username}</span><b className="text-brand">{row.points}</b></div>)}</div></section></div></main>; }
