import { ObjectId } from "mongodb";
import { getDb } from "@/lib/db/mongo";
import { currentUserId } from "@/lib/auth/session";
import { LeaderboardView } from "@/components/LeaderboardView";
export const dynamic = "force-dynamic";
type Row={userId:string;points:number;predictions:number;exact:number;username:string;avatarUrl:string|null};
async function leaderboard():Promise<Row[]>{const db=await getDb();const stats=await db.collection<Record<string,unknown>>("leaderboardStats").find({scope:{$in:["GLOBAL","global","all"]}}).sort({points:-1,totalPoints:-1}).limit(100).toArray();const base=stats.length?stats.map((item)=>({userId:String(item.userId??""),points:Number(item.points??item.totalPoints??0),predictions:Number(item.predictions??item.predictionCount??0),exact:Number(item.exact??item.exactScores??0)})):await db.collection<{userId:string}>("predictions").aggregate<{_id:string;predictions:number}>([{$match:{userId:{$ne:"guest"}}},{$group:{_id:"$userId",predictions:{$sum:1}}},{$sort:{predictions:-1}},{$limit:100}]).toArray().then((items)=>items.map((item)=>({userId:item._id,points:0,predictions:item.predictions,exact:0})));const ids=base.map((item)=>item.userId).filter(ObjectId.isValid).map((id)=>new ObjectId(id));const users=ids.length?await db.collection<{username:string;avatarUrl?:string|null}>("users").find({_id:{$in:ids}},{projection:{username:1,avatarUrl:1}}).toArray():[];const byId=new Map(users.map((user)=>[String(user._id),user]));return base.map((item)=>({...item,username:byId.get(item.userId)?.username??"LowBlock Player",avatarUrl:byId.get(item.userId)?.avatarUrl??null}));}
export default async function LeaderboardPage(){const[rows,me]=await Promise.all([leaderboard(),currentUserId()]);return <LeaderboardView rows={rows} me={me}/>}
