"use client";
import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import { CheckCircle2, Link2 } from "lucide-react";
import { useLanguage } from "@/components/LanguageProvider";
export default function InvitePage({ params }: { params: Promise<{ token: string }> }) { const router = useRouter(); const { t } = useLanguage(); const [token, setToken] = useState(""); const [message, setMessage] = useState(""); const [busy, setBusy] = useState(false); useEffect(() => { params.then(({ token: value }) => setToken(value)); }, [params]); async function redeem(confirmSwitch = false) { setBusy(true); const response = await fetch("/api/invitations/redeem", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ token, confirmSwitch }) }); const data = await response.json(); if (response.status === 409 && data.requiresSwitchConfirmation) { setBusy(false); if (confirm(t("پذیرش این دعوت باشگاه فعلی شما را تغییر می‌دهد. ادامه می‌دهید؟", "Accepting this invitation will change your current Club. Continue?"))) return redeem(true); return; } if (!response.ok) { setMessage(data.error ?? "Could not redeem invitation"); setBusy(false); return; } setMessage(t("به باشگاه پیوستید.", "You joined the Club.")); setTimeout(() => router.replace("/club"), 700); } return <main className="grid min-h-screen place-items-center px-5 pb-28 pt-24"><section className="w-full max-w-md rounded-[2rem] border border-white/10 bg-[radial-gradient(circle_at_90%_0%,rgba(32,184,121,.22),transparent_40%),linear-gradient(145deg,#14251c,#0a0f0c)] p-8 text-center"><div className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-brand/15 text-brand"><Link2 size={25}/></div><h1 className="mt-5 text-3xl font-black">{t("دعوت باشگاه", "Club invitation")}</h1><p className="mt-3 text-sm leading-7 text-[var(--muted)]">{t("با پذیرش دعوت، پیش‌بینی‌های شما همچنان از همان صفحه مرکزی ثبت می‌شوند.", "Your predictions will still be submitted from the central Predict page.")}</p><button disabled={busy || !token} onClick={() => redeem()} className="mt-7 inline-flex w-full items-center justify-center gap-2 rounded-xl bg-brand px-5 py-3 font-black">{busy ? "…" : <><CheckCircle2 size={17}/>{t("پذیرش دعوت", "Accept invitation")}</>}</button>{message && <p className="mt-4 rounded-xl bg-brand/10 p-3 text-sm text-brand">{message}</p>}</section></main>; }
