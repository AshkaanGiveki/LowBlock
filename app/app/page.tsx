import Link from "next/link"; import { ArrowLeft, CalendarDays, ChartNoAxesCombined, Trophy } from "lucide-react"; import { Nav } from "@/components/Nav"; import { LEAGUES } from "@/lib/football/leagues";
export default function Dashboard(){return <><Nav/><main className="mx-auto max-w-7xl px-5 pb-20 md:px-10"><div className="mb-8 flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="text-sm text-[var(--muted)]">شنبه، ۲۵ مرداد ۱۴۰۵</p><h1 className="mt-2 text-3xl font-black">سلام، فوتبال‌دوست 👋</h1></div><Link href="/leagues/GB1" className="inline-flex items-center gap-2 self-start rounded-xl bg-brand px-4 py-3 text-sm font-bold text-ink">رفتن به مسابقات <ArrowLeft size={17}/></Link></div><div className="grid gap-3 sm:grid-cols-3"><Stat icon={<Trophy/>} label="امتیاز کل" value="—"/><Stat icon={<ChartNoAxesCombined/>} label="رتبه جهانی" value="—"/><Stat icon={<CalendarDays/>} label="پیش‌بینی‌ها" value="—"/></div><div className="mt-8 grid gap-5 lg:grid-cols-[1.4fr_.6fr]"><section className="panel min-h-64 p-6"><div className="flex items-center justify-between"><h2 className="font-bold">مسابقات پیش‌رو</h2><span className="text-xs text-[var(--muted)]">پس از همگام‌سازی نمایش داده می‌شود</span></div><Empty/></section><section className="panel p-6"><h2 className="font-bold">لیگ‌ها</h2><div className="mt-4 space-y-2">{LEAGUES.map(l=><Link key={l.code} href={`/leagues/${l.code}`} className="flex items-center justify-between rounded-xl px-3 py-3 text-sm transition hover:bg-white/5"><span>{l.faName}</span><ArrowLeft size={15} className="text-[var(--muted)]"/></Link>)}</div></section></div></main></>}
function Stat({icon,label,value}:{icon:React.ReactNode;label:string;value:string}){return <div className="panel flex items-center gap-4 p-5"><span className="text-brand">{icon}</span><div><p className="text-xs text-[var(--muted)]">{label}</p><p className="mt-1 text-2xl font-black">{value}</p></div></div>}; function Empty(){return <div className="grid min-h-48 place-items-center text-center text-sm text-[var(--muted)]"><div><div className="mx-auto mb-3 grid h-12 w-12 place-items-center rounded-2xl bg-brand-subtle text-brand">✦</div><p>هنوز داده‌ای برای نمایش نیست.</p><p className="mt-1 text-xs">مسابقات واقعی پس از اتصال منبع داده همگام می‌شوند.</p></div></div>}
