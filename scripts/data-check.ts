import { env } from "../lib/env"; import { closeMongo, getDb } from "../lib/db/mongo"; import { LEAGUES } from "../lib/football/leagues";
async function main(){try{if(!env.MONGODB_URI)throw new Error("MONGODB_URI is required");const db=await getDb();const leagues=await db.collection("leagues").countDocuments({code:{$in:LEAGUES.map(l=>l.code)}},{maxTimeMS:10_000});const teams=await db.collection("teams").estimatedDocumentCount({maxTimeMS:10_000});const matches=await db.collection("matches").estimatedDocumentCount({maxTimeMS:10_000});console.log(JSON.stringify({database:db.databaseName,leagues,teams,matches},null,2));}finally{await closeMongo();}}main().catch(e=>{console.error(e instanceof Error?e.message:"check failed");process.exit(1)});
