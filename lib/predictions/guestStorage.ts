export const GUEST_PREDICTIONS_KEY = "lowblock_guest_predictions_v1";
export type StoredGuestPrediction = { receipt: string; matchId: string; homeGoals: number; awayGoals: number; createdAt: number };

export function readGuestPredictions(): StoredGuestPrediction[] { if (typeof window === "undefined") return []; try { const raw = window.localStorage.getItem(GUEST_PREDICTIONS_KEY); const rows = JSON.parse(raw ?? "[]"); return Array.isArray(rows) ? rows.filter((row) => row && typeof row.receipt === "string" && typeof row.matchId === "string") : []; } catch { return []; } }
export function writeGuestPrediction(row: StoredGuestPrediction): boolean { try { const rows = readGuestPredictions().filter((item) => item.matchId !== row.matchId); window.localStorage.setItem(GUEST_PREDICTIONS_KEY, JSON.stringify([...rows, row])); return true; } catch { return false; } }
export function removeGuestPredictions(matchIds: string[]) { try { const ids = new Set(matchIds); window.localStorage.setItem(GUEST_PREDICTIONS_KEY, JSON.stringify(readGuestPredictions().filter((row) => !ids.has(row.matchId)))); } catch { /* private browsing can reject writes */ } }
export function removeGuestReceipts(receipts: string[]) { try { const values = new Set(receipts); window.localStorage.setItem(GUEST_PREDICTIONS_KEY, JSON.stringify(readGuestPredictions().filter((row) => !values.has(row.receipt)))); } catch { /* private browsing can reject writes */ } }
export async function migrateGuestPredictions() { const rows=readGuestPredictions(); if(!rows.length)return {imported:[],handled:[]}; try { const response=await fetch("/api/predictions/import",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({receipts:rows.map(row=>row.receipt)})}); if(!response.ok)return {imported:[],handled:[]}; const data=await response.json(); removeGuestPredictions(data.handled??[]); removeGuestReceipts(data.discarded??[]); return data; } catch { return {imported:[],handled:[]}; } }
