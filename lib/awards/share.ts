export const LOWBLOCK_HOME = "https://lowblock.ir";
export const awardShareText = "I won this round on Low Block. ⚽";
export async function shareAward(shareImageUrl: string) { const response = await fetch(shareImageUrl); if (!response.ok) throw new Error("Share image unavailable"); const blob = await response.blob(); const file = new File([blob], "lowblock-round-winner.png", { type: blob.type || "image/png" }); if (navigator.share && navigator.canShare?.({ files: [file] })) { await navigator.share({ files: [file], text: `${awardShareText}\n\n${LOWBLOCK_HOME}`, url: LOWBLOCK_HOME }); return "shared" as const; } const objectUrl = URL.createObjectURL(blob); const link = document.createElement("a"); link.href = objectUrl; link.download = file.name; link.click(); URL.revokeObjectURL(objectUrl); await navigator.clipboard?.writeText(LOWBLOCK_HOME).catch(() => undefined); return "saved" as const; }
