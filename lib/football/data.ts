import { getDb } from "@/lib/db/mongo";
export type MatchRecord={_id?:unknown;providerMatchId:string;leagueCode:string;matchday:number;kickoffAt:Date;status:string;homeGoals:number|null;awayGoals:number|null;homeTeam:{id:number;name:string;logoUrl:string|null};awayTeam:{id:number;name:string;logoUrl:string|null};seasonStartYear:number};
export async function getMatches(filters:{leagueCode?:string;matchday?:number;limit?:number}={}){const db=await getDb(); const query={provider:"football-api",...(filters.leagueCode?{leagueCode:filters.leagueCode}:{}),...(filters.matchday!==undefined?{matchday:filters.matchday}:{})}; return db.collection<MatchRecord>("matches").find(query).sort({kickoffAt:1}).limit(filters.limit??50).toArray();}
export async function getPredictions(matchIds:string[],userId="guest"){if(!matchIds.length)return new Map(); const db=await getDb(); const rows=await db.collection<{matchId:string;homeGoals:number;awayGoals:number}> ("predictions").find({userId,matchId:{$in:matchIds}}).toArray(); return new Map(rows.map(row=>[row.matchId,row]));}
