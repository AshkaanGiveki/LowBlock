import { env } from "@/lib/env"; import { isTransfermarktChallenge } from "./challenge-detection";
export async function fetchTransfermarkt(url:string,signal?:AbortSignal){const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),12_000);try{const res=await fetch(url,{signal:signal??controller.signal,headers:{"User-Agent":"LowBlock/1.0 (+respectful public-data client)"}});if(!res.ok)throw new Error(`TRANSFERMARKT_HTTP_${res.status}`);const html=await res.text();if(isTransfermarktChallenge(html))throw new Error("TRANSFERMARKT_CHALLENGE");return html;}finally{clearTimeout(timer);}}
